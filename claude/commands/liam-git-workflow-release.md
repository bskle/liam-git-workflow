---
description: Prepare release guidance for merging dev to main and tagging a release.
---

Read:

- `@{{REFERENCES_ROOT}}/policy.md`
- `@{{REFERENCES_ROOT}}/pr-rules.md`

The user wants release guidance:

`$ARGUMENTS`

Workflow:

1. confirm the release context
2. explain the `dev` -> `main` path
3. recommend the tag shape `vMAJOR.MINOR.PATCH`
4. remind about release notes and verification

Output:

- release branch or direct merge recommendation
- merge target
- tag recommendation
- release checklist
