---
phase: quick-o4w-superpowers
plan: 01
name: 技能名称缩短 — 对标 superpowers 命名模式
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/gw/SKILL.md
  - skills/gw-bootstrap/SKILL.md
  - skills/gw-help/SKILL.md
  - skills/gw-commit/SKILL.md
  - skills/gw-create-branch/SKILL.md
  - skills/gw-sync/SKILL.md
  - skills/gw-finish/SKILL.md
  - skills/gw-hotfix/SKILL.md
  - skills/gw-release/SKILL.md
  - skills/gw-sync-policy/SKILL.md
  - skills/gw-diagnose/SKILL.md
  - hooks/hooks.json
autonomous: true
requirements: ["QUICK-o4w-superpowers"]

must_haves:
  truths:
    - "输入 /gw 即可调用主路由技能（原 /liam-git-workflow）"
    - "输入 /gw-commit 即可调用提交技能（原 /liam-git-workflow-commit）"
    - "所有斜杠命令长度从 22-37 字符缩短到 4-16 字符"
    - "技能间交叉引用全部更新为新的短名称，无残留旧名"
    - "Bootstrap 技能清单表格使用新的短名称和短命令"
  artifacts:
    - path: "skills/gw/SKILL.md"
      provides: "主路由技能，含全部 10 个子技能的短名称路由规则"
    - path: "skills/gw-bootstrap/SKILL.md"
      provides: "元技能清单，表格全部 10 行使用新名称和短命令"
    - path: "skills/gw-help/SKILL.md"
      provides: "帮助技能，技能列表全部使用新 $gw-* 格式"
    - path: "hooks/hooks.json"
      provides: "SessionStart 消息引导使用新的短命令"
  key_links:
    - from: "skills/gw/SKILL.md 路由规则"
      to: "skills/gw-*/ 子技能目录"
      via: "技能名称字符串（如 gw-commit 路由到 skills/gw-commit/）"
      pattern: "route to `gw-(commit|help|create-branch|sync|finish|hotfix|release|sync-policy|diagnose)`"
    - from: "skills/gw-bootstrap/SKILL.md 技能清单表格"
      to: "各子技能 SKILL.md frontmatter name 字段"
      via: "技能名称 + 斜杠命令列"
      pattern: "/gw-(commit|help|create-branch|sync|finish|hotfix|release|sync-policy|diagnose|bootstrap)"
    - from: "skills/gw-sync/SKILL.md Troubleshooting 节"
      to: "skills/gw-diagnose/SKILL.md"
      via: "技能名称字符串"
      pattern: "Route to `gw-diagnose`"
    - from: "hooks/hooks.json SessionStart"
      to: "skills/gw-bootstrap/, skills/gw/"
      via: "additionalContext 消息文本中的 /gw-* 命令名"
      pattern: "/gw-bootstrap|/gw"
---

<objective>
将 11 个技能目录名和 frontmatter `name` 字段从 `liam-git-workflow(-*)` 缩短为 `gw(-*)` 模式。
斜杠命令长度缩减 60-80%（如 `/liam-git-workflow-commit` 29 字符 → `/gw-commit` 10 字符）。
对标 superpowers 的短名称风格（`/superpowers:brainstorming`），在不影响功能的条件下优化输入体验。

Purpose: 让用户在 Claude Code 中输入 `/` 后更快选择命令，减少打字成本
Output: 11 个重命名的技能目录 + 全部交叉引用更新 + hooks.json 更新
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
## 命名映射表

| # | 旧目录名 | 新目录名 | 旧命令 | 新命令 | 字符数变化 |
|---|---------|---------|--------|--------|----------|
| 1 | `liam-git-workflow` | `gw` | `/liam-git-workflow` (22) | `/gw` (4) |
| 2 | `liam-git-workflow-bootstrap` | `gw-bootstrap` | `/liam-git-workflow-bootstrap` (33) | `/gw-bootstrap` (14) |
| 3 | `liam-git-workflow-help` | `gw-help` | `/liam-git-workflow-help` (27) | `/gw-help` (9) |
| 4 | `liam-git-workflow-commit` | `gw-commit` | `/liam-git-workflow-commit` (29) | `/gw-commit` (10) |
| 5 | `liam-git-workflow-create-branch` | `gw-create-branch` | `/liam-git-workflow-create-branch` (36) | `/gw-create-branch` (16) |
| 6 | `liam-git-workflow-sync-branch` | `gw-sync` | `/liam-git-workflow-sync-branch` (34) | `/gw-sync` (8) |
| 7 | `liam-git-workflow-finish` | `gw-finish` | `/liam-git-workflow-finish` (29) | `/gw-finish` (10) |
| 8 | `liam-git-workflow-hotfix` | `gw-hotfix` | `/liam-git-workflow-hotfix` (29) | `/gw-hotfix` (10) |
| 9 | `liam-git-workflow-release` | `gw-release` | `/liam-git-workflow-release` (30) | `/gw-release` (10) |
| 10 | `liam-git-workflow-sync-policy` | `gw-sync-policy` | `/liam-git-workflow-sync-policy` (33) | `/gw-sync-policy` (14) |
| 11 | `liam-git-workflow-remote-diagnose` | `gw-diagnose` | `/liam-git-workflow-remote-diagnose` (37) | `/gw-diagnose` (12) |

## 关键约束

- Claude Code 将 `skills/` 下的每个子目录名自动注册为斜杠命令
- plugin.json `"name": "liam-git-workflow"` 保持不变（插件标识）
- references/ 目录下的政策文件**不修改**（纯内容文件，不含技能名称引用）
- 不影响任何功能逻辑，仅改名

## 交叉引用影响范围

### 需要更新的文件（按修改量排序）

1. **skills/gw-bootstrap/SKILL.md** — 31 处 `liam-git-workflow` 出现，含技能清单表格（10 行 x 3 列）、路由优先级、使用示例、Red Flags 表格
2. **skills/gw/SKILL.md** — 12 处，含全部路由规则（每个 `route to` 指令引用子技能名）
3. **skills/gw-help/SKILL.md** — 12 处，含 10 个 `$liam-git-workflow-*` 入口列表
4. **skills/gw-sync/SKILL.md** — 2 处，Troubleshooting 节引用 `gw-diagnose`
5. **skills/gw-diagnose/SKILL.md** — 1 处，Manual Trigger 节引用自身名
6. **hooks/hooks.json** — 2 处，SessionStart additionalContext 消息中 `/liam-git-workflow-bootstrap` 和 `/liam-git-workflow`
7. **其余 6 个子技能 SKILL.md** — 仅 frontmatter `name` 字段各 1 处（gw-commit, gw-create-branch, gw-finish, gw-hotfix, gw-release, gw-sync-policy）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重命名目录 + 更新 frontmatter name</name>
  <files>
    skills/gw/SKILL.md
    skills/gw-bootstrap/SKILL.md
    skills/gw-help/SKILL.md
    skills/gw-commit/SKILL.md
    skills/gw-create-branch/SKILL.md
    skills/gw-sync/SKILL.md
    skills/gw-finish/SKILL.md
    skills/gw-hotfix/SKILL.md
    skills/gw-release/SKILL.md
    skills/gw-sync-policy/SKILL.md
    skills/gw-diagnose/SKILL.md
  </files>
  <action>
**步骤 1: git mv 所有 11 个目录**

使用 `git mv` 按映射表逐对重命名，保留 git 历史追踪：

```bash
git mv skills/liam-git-workflow skills/gw
git mv skills/liam-git-workflow-bootstrap skills/gw-bootstrap
git mv skills/liam-git-workflow-help skills/gw-help
git mv skills/liam-git-workflow-commit skills/gw-commit
git mv skills/liam-git-workflow-create-branch skills/gw-create-branch
git mv skills/liam-git-workflow-sync-branch skills/gw-sync
git mv skills/liam-git-workflow-finish skills/gw-finish
git mv skills/liam-git-workflow-hotfix skills/gw-hotfix
git mv skills/liam-git-workflow-release skills/gw-release
git mv skills/liam-git-workflow-sync-policy skills/gw-sync-policy
git mv skills/liam-git-workflow-remote-diagnose skills/gw-diagnose
```

注意：`git mv` 在同级目录下操作（skills/old → skills/new），先执行所有 mv 命令，一次性验证。

**步骤 2: 更新每个 SKILL.md 的 frontmatter `name` 字段**

按映射表逐个修改。11 个文件，每个修改 1 行（`name:` 后的值）。

| 文件 | 旧 name | 新 name |
|------|---------|---------|
| skills/gw/SKILL.md | liam-git-workflow | gw |
| skills/gw-bootstrap/SKILL.md | liam-git-workflow-bootstrap | gw-bootstrap |
| skills/gw-help/SKILL.md | liam-git-workflow-help | gw-help |
| skills/gw-commit/SKILL.md | liam-git-workflow-commit | gw-commit |
| skills/gw-create-branch/SKILL.md | liam-git-workflow-create-branch | gw-create-branch |
| skills/gw-sync/SKILL.md | liam-git-workflow-sync-branch | gw-sync |
| skills/gw-finish/SKILL.md | liam-git-workflow-finish | gw-finish |
| skills/gw-hotfix/SKILL.md | liam-git-workflow-hotfix | gw-hotfix |
| skills/gw-release/SKILL.md | liam-git-workflow-release | gw-release |
| skills/gw-sync-policy/SKILL.md | liam-git-workflow-sync-policy | gw-sync-policy |
| skills/gw-diagnose/SKILL.md | liam-git-workflow-remote-diagnose | gw-diagnose |

每个文件的修改：将 frontmatter 区第二行 `name: liam-git-workflow-*` 替换为 `name: gw-*`。
</action>
  <verify>
    <automated>
      # 验证 11 个新目录存在
      test $(ls -d skills/gw skills/gw-*/ 2>/dev/null | wc -l) -eq 11

      # 验证 11 个旧目录已不存在
      test $(ls -d skills/liam-git-workflow* 2>/dev/null | wc -l) -eq 0

      # 验证所有 frontmatter name 字段已更新（无残留 liam-git-workflow 开头的 name）
      grep -rn "^name: liam-git-workflow" skills/gw*/SKILL.md 2>/dev/null | wc -l | xargs -I{} test {} -eq 0

      # 验证所有 frontmatter name 字段以 gw 开头
      grep -rn "^name: gw" skills/gw*/SKILL.md 2>/dev/null | wc -l | xargs -I{} test {} -eq 11
    </automated>
  </verify>
  <done>
    11 个目录已通过 git mv 重命名，git 历史保留。
    11 个 SKILL.md frontmatter `name` 字段全部更新为新名称。
    旧目录 `skills/liam-git-workflow*` 全部消失，新目录 `skills/gw*` 全部存在。
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新 body 交叉引用 + hooks.json</name>
  <files>
    skills/gw/SKILL.md
    skills/gw-bootstrap/SKILL.md
    skills/gw-help/SKILL.md
    skills/gw-sync/SKILL.md
    skills/gw-diagnose/SKILL.md
    hooks/hooks.json
  </files>
  <action>
**步骤 1: skills/gw/SKILL.md — 更新主路由中的 10 条子技能引用**

在 Routing Rules 节中，每条 `route to \`liam-git-workflow-*\`` 改为 `route to \`gw-*\``：

```
route to `liam-git-workflow-help`          → route to `gw-help`
route to `liam-git-workflow-create-branch`  → route to `gw-create-branch`
route to `liam-git-workflow-commit`         → route to `gw-commit`
route to `liam-git-workflow-sync-branch`    → route to `gw-sync`
route to `liam-git-workflow-finish`         → route to `gw-finish`
route to `liam-git-workflow-hotfix`         → route to `gw-hotfix`
route to `liam-git-workflow-release`        → route to `gw-release`
route to `liam-git-workflow-sync-policy`    → route to `gw-sync-policy`
route to `liam-git-workflow-remote-diagnose` → route to `gw-diagnose`
```

**注意：** 第 10 条 remote-diagnose 出现 2 次（触发条件 + 自动触发），两处都要更新。

Response Style 节末尾 `$liam-git-workflow-create-branch` → `$gw-create-branch`。

**步骤 2: skills/gw-bootstrap/SKILL.md — 更新技能清单表格、路由优先级、使用示例、Red Flags**

这是修改量最大的文件（31 处 `liam-git-workflow`）：

- **Section 2「1% 规则」** 第 1 段：`liam-git-workflow` → `gw`（表述为「不替代 `gw` 主路由技能」）
- **Section 3「技能清单」表格**：10 行全部替换
  - 「技能名称」列（第一列）：`liam-git-workflow` → `gw`, `liam-git-workflow-help` → `gw-help`, ...
  - 「斜杠命令」列（第二列）：`/liam-git-workflow` → `/gw`, `/liam-git-workflow-help` → `/gw-help`, ...
  - 「触发场景」列中有 `liam-git-workflow` 主路由引用（第 1 行 "不确定用哪个技能" 对应的列 → 变更说明为"自然语言 Git 请求由主路由 `gw` 自动判断"），以及 remote-diagnose 的触发场景中提到 `liam-git-workflow` 主路由
  - **具体每行的第三列修改：**
    - 第 1 行「`liam-git-workflow` 主路由自动判断」→「`gw` 主路由自动判断」
    - 第 9 行 → 不变（sync-policy 触发场景不含技能名引用）
- **Section 4「路由优先级」**：3 处引用
  - `liam-git-workflow (主路由)` → `gw (主路由)`
  - `liam-git-workflow` 主路由 → `gw` 主路由（末尾句）
  - `liam-git-workflow-help` → `gw-help`
- **Section 5「Red Flags」表格**：5 行，每行 "正确做法" 列含技能名
  - `liam-git-workflow-commit` → `gw-commit`
  - `liam-git-workflow-remote-diagnose` → `gw-diagnose`
  - 其他行不含直接技能名引用 → 不变
- **Section 6「使用示例」**：4 个示例
  - 示例 1: `liam-git-workflow-commit` → `gw-commit`（3 处），斜杠命令 `/liam-git-workflow-commit` → `/gw-commit`
  - 示例 2: `liam-git-workflow` 主路由 → `gw` 主路由（2 处），`liam-git-workflow-release` → `gw-release`，斜杠命令 `/liam-git-workflow-release` → `/gw-release`
  - 示例 3: `liam-git-workflow-remote-diagnose` → `gw-diagnose`（2 处），斜杠命令 `/liam-git-workflow-remote-diagnose` → `/gw-diagnose`
  - 示例 4: `liam-git-workflow-help` → `gw-help`，斜杠命令 `/liam-git-workflow-bootstrap` → `/gw-bootstrap`
- **Section 7「与主路由的关系」**：2 处
  - `liam-git-workflow` 是路由层 → `gw` 是路由层
  - `liam-git-workflow` 主路由 → `gw` 主路由

**步骤 3: skills/gw-help/SKILL.md — 更新技能列表**

Section「List the available entries」中的 10 个 `$liam-git-workflow-*` 全部替换为 `$gw-*`：
```
$liam-git-workflow               → $gw
$liam-git-workflow-help           → $gw-help
$liam-git-workflow-create-branch  → $gw-create-branch
$liam-git-workflow-commit         → $gw-commit
$liam-git-workflow-sync-branch    → $gw-sync
$liam-git-workflow-finish         → $gw-finish
$liam-git-workflow-hotfix         → $gw-hotfix
$liam-git-workflow-release        → $gw-release
$liam-git-workflow-sync-policy    → $gw-sync-policy
$liam-git-workflow-remote-diagnose → $gw-diagnose
```

Section「Explain」中第 1 条 `$liam-git-workflow` → `$gw`。

**步骤 4: skills/gw-sync/SKILL.md — 更新 Troubleshooting 交叉引用**

Troubleshooting 节中 2 处引用：
- `liam-git-workflow-remote-diagnose` → `gw-diagnose`（步骤 1 和第 5 行 Common remote failures 上方文本中各 1 处）

**步骤 5: skills/gw-diagnose/SKILL.md — 更新 Manual Trigger 自引用**

Manual Trigger 节中 `liam-git-workflow-remote-diagnose` → `gw-diagnose`（1 处）。

**步骤 6: hooks/hooks.json — 更新 SessionStart 消息**

`additionalContext` 字符串中：
- `/liam-git-workflow-bootstrap` → `/gw-bootstrap`
- `/liam-git-workflow` → `/gw`

修改后立即用 PowerShell 验证 JSON 语法：`powershell -NoProfile -Command "Get-Content hooks/hooks.json | ConvertFrom-Json | Out-Null; Write-Host 'JSON valid'"`

**全局验证：** 修改完成后，在 skills/ 目录下搜索残留的 `liam-git-workflow` 字符串：
```bash
grep -rn "liam-git-workflow" skills/gw*/SKILL.md --include="*.md" 2>/dev/null
```
预期结果：零匹配。若有残留，逐一手动修复。
</action>
  <verify>
    <automated>
      # 无残留旧名引用
      grep -rn "liam-git-workflow" skills/gw*/SKILL.md 2>/dev/null | wc -l | xargs -I{} test {} -eq 0

      # hooks.json 中无残留旧命令名
      grep -c "/liam-git-workflow" hooks/hooks.json | xargs -I{} test {} -eq 0

      # hooks.json 中有新的短命令名
      grep -c "/gw-bootstrap" hooks/hooks.json | xargs -I{} test {} -gt 0
      grep -c "/gw\b" hooks/hooks.json | xargs -I{} test {} -gt 0

      # hooks.json JSON 语法有效
      powershell -NoProfile -Command "Get-Content hooks/hooks.json | ConvertFrom-Json | Out-Null; Write-Host 'valid'"

      # 主路由引用全部更新为 gw-* 格式
      grep -c "route to \`gw-" skills/gw/SKILL.md | xargs -I{} test {} -ge 9

      # Bootstrap 表格中 10 行全部使用 gw-* 名称
      grep -c "\`gw" skills/gw-bootstrap/SKILL.md | xargs -I{} test {} -ge 10
    </automated>
  </verify>
  <done>
    所有 SKILL.md body 文本中的 `liam-git-workflow-*` 交叉引用全部替换为 `gw-*`。
    hooks.json SessionStart 消息使用新的短命令名。
    JSON 语法验证通过。
    全局搜索 skills/ 目录下零残留旧名。
  </done>
</task>

</tasks>

<verification>
## 整体验证

修改完成后执行以下一键验证脚本：

```bash
echo "=== 目录结构 ==="
ls -d skills/gw*/ 2>/dev/null
echo "---"
echo "旧目录残留检查:"
ls -d skills/liam-git-workflow* 2>/dev/null && echo "FAIL: 旧目录残留" || echo "PASS"
echo "---"
echo "旧名引用残留检查:"
grep -rn "liam-git-workflow" skills/gw*/SKILL.md 2>/dev/null && echo "FAIL: 代码中有旧名残留" || echo "PASS"
grep "liam-git-workflow" hooks/hooks.json && echo "FAIL: hooks.json 有旧名残留" || echo "PASS"
echo "---"
echo "hooks.json JSON语法检查:"
powershell -NoProfile -Command "try { Get-Content hooks/hooks.json | ConvertFrom-Json | Out-Null; Write-Host 'PASS' } catch { Write-Host 'FAIL' }"
echo "---"
echo "fw-bootstrap 表格完整性检查:"
grep -c "| \`gw" skills/gw-bootstrap/SKILL.md
```

预期输出：
- 11 个 `skills/gw*/` 目录
- 0 个旧目录（PASS）
- 0 个旧名残留（PASS）
- hooks.json 语法有效（PASS）
- bootstrap 表格至少 10 处 `gw` 前缀引用
</verification>

<success_criteria>
- [ ] 11 个技能目录已从 `liam-git-workflow-*` 重命名为 `gw-*`（git mv，历史保留）
- [ ] 11 个 SKILL.md frontmatter `name` 字段全部使用新短名称
- [ ] 主路由 `gw` 的 10 条路由规则全部引用新的 `gw-*` 子技能名
- [ ] Bootstrap 技能清单表格 10 行全部使用新名称和新斜杠命令
- [ ] Help 技能列表 10 个入口全部使用 `$gw-*` 格式
- [ ] sync-branch 到 remote-diagnose 的交叉引用已更新
- [ ] hooks.json SessionStart 消息使用 `/gw-bootstrap` 和 `/gw`
- [ ] 全局搜索 skills/ 和 hooks/ 下零残留 `liam-git-workflow` 字符串
- [ ] 所有功能逻辑不变，仅名称变更
</success_criteria>

<output>
After completion, create `.planning/quick/260603-o4w-superpowers-liam-git-workflow/260603-o4w-SUMMARY.md`
</output>
