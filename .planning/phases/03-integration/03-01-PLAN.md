---
phase: 03-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/liam-git-workflow/SKILL.md
  - references/scenarios.md
  - skills/liam-git-workflow-sync-branch/SKILL.md
autonomous: true
requirements: [DIAG-05, DIAG-06, DIAG-07]

must_haves:
  truths:
    - "当用户描述 push/pull/fetch/ls-remote 失败时，主工作流技能路由到远程诊断能力"
    - "当执行期间远程 Git 操作失败（非零退出码）时，主工作流技能路由到远程诊断能力"
    - "场景文档包含 push、pull、fetch、ls-remote 各至少一个远程故障场景示例"
    - "同步分支技能在远程操作失败时引用远程诊断能力"
  artifacts:
    - path: "skills/liam-git-workflow/SKILL.md"
      provides: "远程故障路由规则（DIAG-05）"
      contains: "liam-git-workflow-remote-diagnose"
    - path: "references/scenarios.md"
      provides: "远程故障场景示例（DIAG-06）"
      contains: "remote-diagnostics.md"
    - path: "skills/liam-git-workflow-sync-branch/SKILL.md"
      provides: "远程诊断引用和故障排除章节（DIAG-07）"
      contains: "liam-git-workflow-remote-diagnose"
  key_links:
    - from: "skills/liam-git-workflow/SKILL.md routing rules"
      to: "liam-git-workflow-remote-diagnose skill"
      pattern: "route to `liam-git-workflow-remote-diagnose`"
    - from: "references/scenarios.md remote failure scenarios"
      to: "references/remote-diagnostics.md"
      pattern: "remote-diagnostics\\.md"
    - from: "skills/liam-git-workflow-sync-branch/SKILL.md"
      to: "liam-git-workflow-remote-diagnose"
      pattern: "liam-git-workflow-remote-diagnose"
---

<objective>
将 Phase 1 和 Phase 2 构建的远程诊断能力接入现有工作流系统。更新三个文件：主线工作流技能的路由规则、场景参考文档、以及同步分支技能的故障排除指引。

Purpose: 完成"自动触发 + 手动回退"双入口设计的最后一环 -- 使诊断能力在远程 Git 操作失败时可被发现和触发，而非孤立存在。
Output: 三个已更新的技能/参考文件，使完整工作流闭环（操作失败 → 诊断 → 修复 → 重试）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Extracted interfaces from the diagnostic skill (Phase 1 output):
- **Skill name**: `liam-git-workflow-remote-diagnose`
- **Skill file**: `skills/liam-git-workflow-remote-diagnose/SKILL.md`
- **Reference file**: `references/remote-diagnostics.md`
- **Trigger conditions**: remote commands (push/pull/fetch/ls-remote) returning non-zero exit status, OR known failure patterns in stderr
- **Five diagnostic layers**: 本地状态 → 远程目标 → 认证/授权 → 网络路径 → 仓库策略
- **Output contract**: 8-field structured JSON (problem_category, evidence, likely_cause, confidence, actions_taken, recommended_next_action, human_interaction_required, human_action_detail)

Extracted interfaces from liam-git-workflow/SKILL.md (Phase 3 target):
- **Load First section** (lines 10-18): References policy.md, branch-matrix.md, commit-rules.md, pr-rules.md, scenarios.md
- **Routing Rules section** (lines 20-29): 8 existing routing rules covering branch creation, commit, sync, finish, hotfix, release, policy audit, help
- **Response Style section** (lines 31-39): Style patterns for output

Extracted structure from scenarios.md (Phase 3 target):
- **Format**: `## Scenario name` → Input (code block with `$command`) → Expected classification (bullet list)
- **5 existing scenarios**: hotfix branch, fix branch, commit, finish, policy audit
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为 liam-git-workflow 主技能添加远程故障路由规则 (DIAG-05)</name>
  <files>skills/liam-git-workflow/SKILL.md</files>

  <read_first>
1. Read `skills/liam-git-workflow/SKILL.md` to understand current structure (Load First section at lines 10-18, Routing Rules at lines 20-29)
2. Read `skills/liam-git-workflow-remote-diagnose/SKILL.md` to understand the trigger conditions and skill name for accurate routing rule wording
  </read_first>

  <action>
在 `skills/liam-git-workflow/SKILL.md` 中进行三处编辑：

**编辑 1 -- Load First 区块 (第 12-18 行):**
将 `../../references/remote-diagnostics.md` 追加到文件读取列表末尾。插入位置在第 17 行 `- ../../references/scenarios.md` 之后、第 18 行空行之前。新增行内容：
`- ../../references/remote-diagnostics.md`

**编辑 2 -- Routing Rules 区块 (第 22-29 行):**
在现有 8 条路由规则末尾（第 29 行 `sync-policy` 规则之后），依次追加以下两条规则：

规则 A（用户描述型远程故障）：
`- If the request describes a remote operation failure (push/pull/fetch/ls-remote returning errors like "Authentication failed", "Could not resolve host", "Connection timed out", "protected branch", "repository not found", "Permission denied") or asks to diagnose remote connectivity problems, route to \`liam-git-workflow-remote-diagnose\``

规则 B（执行期间远程失败）：
`- If a remote Git operation (\`push\`, \`pull\`, \`fetch\`, \`ls-remote\`) fails during command execution with a non-zero exit status, stop the current action and route to \`liam-git-workflow-remote-diagnose\` for structured diagnosis before any retry`

**编辑 3 -- Response Style 区块:**
在 Response Style 区块末尾（第 39 行），追加一条远程故障相关响应指引：
`- When a remote operation fails, do not retry the same command immediately — first route to the diagnostic skill, then apply the structured recommendation`

无需添加新的 SKILL.md 子系统依赖声明 -- 现有脚本（`scripts/diagnose_git_remote.ps1`）通过技能路由名称发现。
  </action>

  <acceptance_criteria>
1. `grep -c "liam-git-workflow-remote-diagnose" skills/liam-git-workflow/SKILL.md` returns at least 2 (两条路由规则各出现一次)
2. `grep -c "remote-diagnostics.md" skills/liam-git-workflow/SKILL.md` returns exactly 1 (Load First 区块中的引用)
3. Routing Rules 区块中两条新规则的顺序为：用户描述型路由在前（第 9 条规则）、执行期间失败路由在后（第 10 条规则）
4. 文件末尾的 `Response Style` 区块包含新增的远程故障指引行
5. 文件的 YAML frontmatter 保持完整且未被修改
  </acceptance_criteria>

  <verify>
    <automated>grep -n "liam-git-workflow-remote-diagnose" skills/liam-git-workflow/SKILL.md && echo "---" && grep -n "remote-diagnostics.md" skills/liam-git-workflow/SKILL.md && echo "---" && grep -n "do not retry" skills/liam-git-workflow/SKILL.md</automated>
  </verify>

  <done>
1. Load First 区块包含 `../../references/remote-diagnostics.md` 引用
2. Routing Rules 区块末尾存在两条 `liam-git-workflow-remote-diagnose` 路由规则（用户描述型 + 执行失败型）
3. Response Style 区块包含远程故障重试指引
4. 已有 8 条路由规则保持完整不变
</done>
</task>

<task type="auto">
  <name>Task 2: 为 scenarios.md 补充远程故障场景示例 (DIAG-06)</name>
  <files>references/scenarios.md</files>

  <read_first>
1. Read `references/scenarios.md` to understand the current format convention (## Scenario name → Input code block with $command → Expected classification bullet list)
2. Read `references/remote-diagnostics.md` lines 1-24 (故障分类表) to understand the fault categories and which diagnostic layer each failure type maps to
3. Read `skills/liam-git-workflow-remote-diagnose/SKILL.md` lines 15-33 (Trigger Conditions) to understand automatic trigger patterns for accurate scenario descriptions
  </read_first>

  <action>
在 `references/scenarios.md` 文件末尾（第 74 行 `</code>` 之后）追加 4 个远程故障场景。每个场景遵循现有格式，包含 `##` 标题、`Input:` 代码块、`Expected classification:` 编号列表。

**场景 1 -- Push 失败（受保护分支，第 5 层）：**
```markdown
## Push 失败：受保护分支拒绝推送

Input:

`$liam-git-workflow
git push 失败了，提示 ! [remote rejected] feature/xxx -> feature/xxx (protected branch hook declined)`

Expected classification:

1. problem category: 仓库策略拒绝（第 5 层诊断）
2. trigger signal: stderr 包含 "protected branch" 或 "remote rejected"
3. route to: `liam-git-workflow-remote-diagnose`
4. reference: `references/remote-diagnostics.md` -- 第 5 层「仓库策略与服务端规则」→ 受保护分支信号
5. suggested action: 创建 Pull Request 通过代码审查后合并，这是预期行为而非故障
```

**场景 2 -- Pull 失败（认证错误，第 3 层）：**
```markdown
## Pull 失败：HTTPS 认证凭据过期

Input:

`$liam-git-workflow
git pull 报错了，提示 fatal: Authentication failed for 'https://github.com/xxx/xxx.git/'`

Expected classification:

1. problem category: 认证失败（第 3 层诊断）
2. trigger signal: stderr 包含 "fatal: Authentication failed"
3. route to: `liam-git-workflow-remote-diagnose`
4. reference: `references/remote-diagnostics.md` -- 第 3 层「认证与授权」→ 凭据过期信号
5. suggested action: 使用 `git credential reject` 清除旧凭据后重新执行 pull 触发认证弹窗
```

**场景 3 -- Fetch 失败（DNS 解析，第 4 层）：**
```markdown
## Fetch 失败：DNS 无法解析 Git 服务域名

Input:

`$liam-git-workflow
git fetch 报错了，fatal: unable to access 'https://github.com/xxx/xxx.git/': Could not resolve host: github.com`

Expected classification:

1. problem category: DNS 解析失败（第 4 层诊断）
2. trigger signal: stderr 包含 "Could not resolve host"
3. route to: `liam-git-workflow-remote-diagnose`
4. reference: `references/remote-diagnostics.md` -- 第 4 层「网络路径」→ DNS 解析失败信号
5. suggested action: 运行诊断脚本自动检查 DNS 配置 -- 脚本会通过 `Resolve-DnsName` 和 `.NET GetHostEntry()` 双通道验证 DNS 可用性
```

**场景 4 -- ls-remote 失败（连接超时，第 4 层）：**
```markdown
## ls-remote 失败：TCP 连接超时无法到达远程

Input:

`$liam-git-workflow
git ls-remote origin 一直超时，报 fatal: unable to access 'https://github.com/xxx/xxx.git/': Failed to connect to github.com port 443: Timed out`

Expected classification:

1. problem category: 连接超时/拒绝（第 4 层诊断）
2. trigger signal: stderr 包含 "Failed to connect" 和 "Timed out"
3. route to: `liam-git-workflow-remote-diagnose`
4. reference: `references/remote-diagnostics.md` -- 第 4 层「网络路径」→ 连接超时信号
5. suggested action: 运行诊断脚本自动检查网络路径 -- 脚本会检查代理配置（Git 代理 + 环境变量 + 注册表系统代理）、TCP 443 可达性、HTTPS 最小探测，定位阻断点
```

四个场景按诊断层排序：第 5 层 → 第 3 层 → 第 4 层 → 第 4 层（与文件格式一致，按严重程度从高层到底层排列）。
  </action>

  <acceptance_criteria>
1. `grep -c "liam-git-workflow-remote-diagnose" references/scenarios.md` returns at least 4 (每个场景各引用一次)
2. `grep -c "remote-diagnostics.md" references/scenarios.md` returns at least 4 (每个场景各引用一次)
3. 四个场景标题分别包含 "Push"、"Pull"、"Fetch"、"ls-remote" 关键字
4. 每个场景的 Expected classification 均包含：problem category（标注诊断层）、trigger signal（stderr 模式）、route to（技能名）、reference（知识库引用）、suggested action（可执行操作）
5. 原有的 5 个非远程场景保持完整不变
6. 新增场景出现在文件末尾，在原 5 个场景之后
  </acceptance_criteria>

  <verify>
    <automated>grep -c "liam-git-workflow-remote-diagnose" references/scenarios.md && echo "---" && grep -c "remote-diagnostics.md" references/scenarios.md && echo "---" && grep "^##.*\(Push\|Pull\|Fetch\|ls-remote\).*失败" references/scenarios.md | wc -l</automated>
  </verify>

  <done>
1. 文件包含 9 个场景（5 个原有 + 4 个新增远程故障场景）
2. 四个远程场景分别覆盖 push（受保护分支）、pull（认证失败）、fetch（DNS 解析失败）、ls-remote（连接超时）
3. 每个场景指向 `liam-git-workflow-remote-diagnose` 和 `references/remote-diagnostics.md`
4. 每个场景的 `suggested action` 是可执行的（PR 合并、凭据清除、DNS 检查、网络诊断）
</done>
</task>

<task type="auto">
  <name>Task 3: 为 sync-branch 技能添加远程诊断引用 (DIAG-07)</name>
  <files>skills/liam-git-workflow-sync-branch/SKILL.md</files>

  <read_first>
1. Read `skills/liam-git-workflow-sync-branch/SKILL.md` to understand current structure (frontmatter, Read first, Workflow, Output)
2. Read `skills/liam-git-workflow-remote-diagnose/SKILL.md` lines 15-33 (Trigger Conditions) to understand automatic trigger patterns relevant to sync operations
3. Read `references/remote-diagnostics.md` lines 1-24 (故障分类表) to understand which failure categories are most relevant to pull/fetch sync operations
  </read_first>

  <action>
在 `skills/liam-git-workflow-sync-branch/SKILL.md` 中进行三处编辑：

**编辑 1 -- Read first 区块 (第 8-11 行):**
在 `- ../../references/branch-matrix.md`（第 11 行）之后，追加一行：
`- ../../references/remote-diagnostics.md`
插入位置在第 12 行空行之前。

**编辑 2 -- Workflow 区块 (第 13-18 行):**
在第 17 行 `4. Warn about uncommitted changes and conflict handling` 的末尾追加一句话。修改后的第 17 行为：
`4. Warn about uncommitted changes and conflict handling; if a remote operation (pull/fetch) fails, stop and route to the remote diagnostic capability before retrying`

**编辑 3 -- 在 Output 区块之后新增 Troubleshooting 区块:**
在第 29 行（`</code>` 或最后一个 `- optional command sequence` 行之后）追加：

```markdown
## Troubleshooting

When a sync operation fails with a Git remote error (push/pull/fetch), do not retry the same command immediately. Instead:

1. Route to `liam-git-workflow-remote-diagnose` for structured five-layer diagnosis
2. Apply the recommended fix from the diagnostic output (problem_category, likely_cause, recommended_next_action)
3. Retry the sync operation only after the root cause has been addressed

Common remote failures during sync include:
- Authentication failures (expired credentials, wrong SSH key) -- 第 3 层
- DNS resolution failures (corporate DNS filtering, hostname typos) -- 第 4 层
- Connection timeouts (firewall blocking, proxy misconfiguration) -- 第 4 层
- Upstream branch gone or renamed -- 第 2 层
- Protected branch or organization policy rejection -- 第 5 层

See `references/remote-diagnostics.md` for the complete signal-to-cause mapping and standard repair actions.
```
  </action>

  <acceptance_criteria>
1. `grep -c "liam-git-workflow-remote-diagnose" skills/liam-git-workflow-sync-branch/SKILL.md` returns at least 1 (Troubleshooting 章节中的引用)
2. `grep -c "remote-diagnostics.md" skills/liam-git-workflow-sync-branch/SKILL.md` returns exactly 2 (Read first 区块 1 次 + Troubleshooting 章节 1 次)
3. Read first 区块包含 `../../references/remote-diagnostics.md` 引用
4. Workflow 区块的第 4 步包含远程失败处理指引
5. 文件末尾存在 `## Troubleshooting` 章节，位于 `## Output` 区块之后
6. Troubleshooting 章节列出至少 5 种常见同步期间远程故障及其诊断层编号
7. 文件的 YAML frontmatter 保持完整且未被修改
  </acceptance_criteria>

  <verify>
    <automated>grep -n "remote-diagnostics.md" skills/liam-git-workflow-sync-branch/SKILL.md && echo "---" && grep -n "liam-git-workflow-remote-diagnose" skills/liam-git-workflow-sync-branch/SKILL.md && echo "---" && grep -n "Troubleshooting" skills/liam-git-workflow-sync-branch/SKILL.md</automated>
  </verify>

  <done>
1. Read first 区块包含 `../../references/remote-diagnostics.md`
2. Workflow 第 4 步末尾包含远程失败时停止并路由到诊断的指引
3. 新增 `## Troubleshooting` 章节在 `## Output` 之后，包含：路由指令（→ liam-git-workflow-remote-diagnose）、5 种常见远程故障及其诊断层、知识库引用
4. 已有 frontmatter、Workflow 1-3 步、Output 区块保持完整不变
</done>
</task>

</tasks>

<verification>
Phase 级别的整体验证：

1. `grep -rl "liam-git-workflow-remote-diagnose" skills/liam-git-workflow/SKILL.md skills/liam-git-workflow-sync-branch/SKILL.md references/scenarios.md | wc -l` 应返回 3（三文件均引用诊断技能）
2. `grep -rl "remote-diagnostics.md" skills/liam-git-workflow/SKILL.md skills/liam-git-workflow-sync-branch/SKILL.md references/scenarios.md | wc -l` 应返回 3（三文件均引用知识库）
3. 主工作流 SKILL.md 的 Routing Rules 区块从 8 条规则扩展到 10 条规则
4. scenarios.md 从 5 个场景扩展到 9 个场景
5. sync-branch SKILL.md 新增 Troubleshooting 章节
</verification>

<success_criteria>
1. DIAG-05 已满足：`skills/liam-git-workflow/SKILL.md` 包含远程故障路由规则（用户描述型 + 执行失败型两条），Load First 区块包含 `remote-diagnostics.md` 引用
2. DIAG-06 已满足：`references/scenarios.md` 包含 push、pull、fetch、ls-remote 各至少一个远程故障场景，每个场景指向 `liam-git-workflow-remote-diagnose` 和 `remote-diagnostics.md`
3. DIAG-07 已满足：`skills/liam-git-workflow-sync-branch/SKILL.md` 包含 `remote-diagnostics.md` 引用和 Troubleshooting 章节，其中引用了 `liam-git-workflow-remote-diagnose`
4. 三文件更新构成完整的诊断能力接入链路：主线路由 → 场景参考 → 子技能故障排除
</success_criteria>

<output>
After completion, create `.planning/phases/03-integration/03-01-SUMMARY.md`
</output>
