---
phase: 05-meta-skill-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/liam-git-workflow-bootstrap/SKILL.md
  - hooks/hooks.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Agent 知道 liam-git-workflow-bootstrap 作为技能发现入口"
    - "Agent 能看到全部 10 个 Git 工作流技能的清单和触发条件"
    - "Agent 在遇到 Git 操作需求时优先查询技能而非手动操作"
    - "会话启动时通过 hooks 上下文提及 bootstrap 技能名称"
    - "Bootstrap 能正确路由用户意图到对应的具体技能"
  artifacts:
    - path: "skills/liam-git-workflow-bootstrap/SKILL.md"
      provides: "元技能引导文件 — 技能清单、1% 规则、路由优先级、Red Flags 表"
      contains: "liam-git-workflow-bootstrap"
      contains: "1% 规则"
      contains: "liam-git-workflow-create-branch"
    - path: "hooks/hooks.json"
      provides: "更新后的 SessionStart 钩子配置，引用 bootstrap 技能名称"
      contains: "liam-git-workflow-bootstrap"
  key_links:
    - from: "hooks/hooks.json additionalContext"
      to: "liam-git-workflow-bootstrap"
      via: "SessionStart 注入文本引导 agent 使用 Skill tool 调用 bootstrap"
      pattern: "liam-git-workflow-bootstrap"
    - from: "bootstrap SKILL.md 技能清单"
      to: "skills/ 目录下 10 个 liam-git-workflow-* 子目录"
      via: "技能名称精确匹配，agent 通过 Skill tool 路由"
      pattern: "liam-git-workflow-"
    - from: "bootstrap SKILL.md 路由规则"
      to: "liam-git-workflow 主路由 SKILL.md 路由规则"
      via: "bootstrap 的触发条件描述与主路由一致，避免两套路由逻辑分歧"
---

<objective>
创建 `liam-git-workflow-bootstrap` 元技能（对标 superpowers 的 `using-superpowers`），实现 Git 工作流技能的自动发现、引导和路由。更新 SessionStart 钩子引用 bootstrap 技能作为入口点。

Purpose: 让 Agent 在会话开始时就能感知所有可用 Git 工作流技能，无需用户记忆具体技能名称。Bootstrap 作为前置发现层，`liam-git-workflow` 主路由负责具体路由。
Output: `skills/liam-git-workflow-bootstrap/SKILL.md` (NEW), `hooks/hooks.json` (MODIFY)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-meta-skill-bootstrap/05-CONTEXT.md

<interfaces>
<!-- Existing hooks.json format (the exact JSON string that needs modification) -->
Current hooks.json SessionStart command output:
```
"additionalContext": "Git workflow policies loaded. Use Skill tool to discover available skills."
```

<!-- Existing skill file pattern (all skills follow this structure) -->
All existing skills use frontmatter:
```yaml
---
name: skill-name
description: Description of what the skill does and when to use it.
---
```
Followed by Markdown sections: `# Title`, `## Subsection`.

<!-- Existing 10 skills with trigger conditions (from liam-git-workflow/SKILL.md routing rules) -->
| 技能名 | 触发条件 |
|--------|---------|
| `liam-git-workflow` | 默认主入口，自然语言 Git 请求 |
| `liam-git-workflow-help` | 询问可用命令/入口 |
| `liam-git-workflow-create-branch` | 创建/命名分支 |
| `liam-git-workflow-commit` | 提交变更 |
| `liam-git-workflow-sync-branch` | 同步最新基线 |
| `liam-git-workflow-finish` | 完成分支收尾 |
| `liam-git-workflow-hotfix` | 生产问题修复 |
| `liam-git-workflow-release` | 打标签/合并 dev 到 main |
| `liam-git-workflow-sync-policy` | 审计 Git 配置与策略 |
| `liam-git-workflow-remote-diagnose` | 远程操作失败诊断 |

<!-- Design decisions from CONTEXT.md -->
D-01: Bootstrap 对标 using-superpowers 的 gatekeeper 角色
D-02: 采用"1% 规则"变体 — 只要用户请求涉及 Git 操作就检查技能
D-03: Bootstrap 不替代 liam-git-workflow 主路由，而是前置发现层
D-04: 通过 hooks/hooks.json SessionStart 注入引导信息
D-05: 不依赖 Claude Code bug #16538 修复 — bootstrap 可通过 Skill tool 被调用
D-06: Bootstrap 列出全部 10 个技能，含触发条件、适用场景、示例
D-07: 路由优先级：bootstrap 引导 → liam-git-workflow 主路由 → 具体子技能
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 liam-git-workflow-bootstrap 元技能 SKILL.md</name>
  <files>skills/liam-git-workflow-bootstrap/SKILL.md (NEW)</files>
  <read_first>
skills/liam-git-workflow/SKILL.md (主路由技能路由规则 — 确保 bootstrap 触发条件与主路由一致)
skills/liam-git-workflow-help/SKILL.md (帮助技能的技能列表 — 确保 bootstrap 清单完整)
  </read_first>
  <action>
创建 `skills/liam-git-workflow-bootstrap/SKILL.md`。这是一个元技能文件，作为 Git 工作流技能的自动发现和路由入口。

### 文件结构要求

Frontmatter:
```yaml
---
name: liam-git-workflow-bootstrap
description: Git 工作流技能自动发现与路由入口。列出全部可用技能、触发条件和路由规则。当 Agent 遇到涉及 Git 操作的用户请求时，优先查询此技能确认是否存在对应的专用技能，避免手动执行 Git 命令。
---
```

正文部分按以下节组织:

### 1. 这是什么

用 2-3 句话说明 bootstrap 是元技能/gatekeeper，对标 superpowers 的 `using-superpowers` 角色。它是前置发现层，不替代 `liam-git-workflow` 主路由技能。当 Agent 被引导到这里后，会自动发现所有可用技能并路由到正确的子技能。

### 2. 1% 规则

内容: 只要用户请求涉及 Git 操作 (分支、提交、同步、发布、配置、故障诊断等)，Agent 有责任检查是否有对应技能。即使只有 1% 的概率匹配，也必须先查询技能清单。禁止 Agent 在未检查技能的情况下直接执行 Git 命令。

列出具体的 Git 操作信号: 分支创建、commit、push、pull、fetch、merge、rebase、tag、hotfix、release、remote error、config audit、工作流咨询等。

### 3. 技能清单

完整的 10 个技能表格，格式:

| 技能名称 | 触发场景 | 典型请求示例 |
|---------|---------|-------------|
| `liam-git-workflow` | 自然语言 Git 请求, 不确定用哪个技能 | "帮我提交代码"、"我要发布" |
| `liam-git-workflow-help` | 询问可用命令列表 | "有哪些 Git 命令可用" |
| `liam-git-workflow-create-branch` | 创建/命名分支 | "我要创建功能分支" |
| `liam-git-workflow-commit` | 提交变更 | "提交当前修改" |
| `liam-git-workflow-sync-branch` | 同步最新基线 | "同步 dev 分支最新代码" |
| `liam-git-workflow-finish` | 完成分支收尾 | "分支做完了下一步是什么" |
| `liam-git-workflow-hotfix` | 生产问题修复 | "线上有 bug 需要紧急修复" |
| `liam-git-workflow-release` | 打标签/合并到 main | "发布版本" "打 tag" |
| `liam-git-workflow-sync-policy` | 审计 Git 配置 | "检查我的 Git 配置是否符合规范" |
| `liam-git-workflow-remote-diagnose` | 远程操作失败诊断 | "push 失败了" "认证错误" |

触发场景的描述必须与 `liam-git-workflow` 主路由的 Routing Rules 中每条规则的触发条件保持一致，避免两套路由逻辑出现分歧。

### 4. 路由优先级

明确路由链: **bootstrap (本技能) → liam-git-workflow (主路由) → 具体子技能**

规则:
- 用户意图模糊 → route to `liam-git-workflow` (主路由自动判断)
- 用户意图明确且匹配某个子技能 → 可直接 route to 具体子技能
- 用户询问有哪些技能可用 → route to `liam-git-workflow-help`
- 任何 Git 操作前 → 先检查本技能清单

说明 bootstrap 不替代主路由: 当 bootstrap 不确定路由到哪个子技能时，应让 `liam-git-workflow` 主路由处理。

### 5. Red Flags — Agent 禁止的借口

表格格式 (对标 using-superpowers):

| Agent 可能说的借口 | 为什么这是错误的 | 正确做法 |
|------------------|-----------------|---------|
| "这是一个简单的 git commit，不需要技能" | 技能确保提交信息格式正确 (中文 + Conventional Commits)，手动操作容易出错 | 使用 `liam-git-workflow-commit` |
| "用户只是想查看分支，不需要技能" | 技能清单中的技能覆盖完整的 Git 工作流 | 检查清单确认场景 |
| "我先执行 git push 看看" | remote-diagnose 技能在 push 失败时提供结构化诊断 | 使用 `liam-git-workflow-remote-diagnose` |
| "我知道这些命令，不需要帮助" | 技能不仅执行命令，还确保策略合规 (分支模型、提交规范) | 始终通过技能路由 |
| "这看起来不需要 bootstrap" | bootstrap 是元技能入口 — 它的存在就是为了被查询 | 遇到 Git 操作先查 bootstrap |

至少列出 5 条常见借口。

### 6. 使用示例

给出 3-4 个具体示例，说明 Agent 如何通过 bootstrap 发现和路由到正确技能:

**示例 1: 提交代码**
- 用户说: "提交当前修改"
- Agent 看到 Git 操作信号 → 检查 bootstrap 技能清单 → 匹配 `liam-git-workflow-commit`
- 调用 `liam-git-workflow-commit` 技能

**示例 2: 不确定操作类型**
- 用户说: "我要发布一个新版本"
- Agent 不确定是 release 还是普通 tag → route to `liam-git-workflow` 主路由
- 主路由判断: 合并 dev→main + tag → 调用 `liam-git-workflow-release`

**示例 3: push 失败**
- Agent 执行 git push 返回认证错误
- Agent 不重试 → 检查 bootstrap 清单 → 匹配 `liam-git-workflow-remote-diagnose`
- 调用诊断技能进行结构化故障排查

**示例 4: 询问可用技能**
- 用户说: "Git 工作流有哪些功能"
- Agent → route to `liam-git-workflow-help`
- Help 技能列出所有入口和用法

### 7. 与 liam-git-workflow 主路由的关系

用 1-2 句明确说明: bootstrap 是发现层 (在 Agent 不知道该用什么技能时被查询)，`liam-git-workflow` 是路由层 (在用户意图不明确时自动判断路由到哪个子技能)。两者互补，bootstrap 确保 agent 知道技能存在，主路由确保路由到正确的技能。

### 文件大小限制

SKILL.md 目标行数: 80-130 行。必须包含所有 10 个技能的清单。不可省略任何技能。

### 验证

创建后确认:
- `grep -c "liam-git-workflow-bootstrap" skills/liam-git-workflow-bootstrap/SKILL.md` 输出 ≥ 1 (frontmatter name)
- `grep -c '1%' skills/liam-git-workflow-bootstrap/SKILL.md` 输出 ≥ 1 (1% 规则)
- `grep -c "liam-git-workflow-create-branch" skills/liam-git-workflow-bootstrap/SKILL.md` 输出 ≥ 1 (技能清单中的具体技能)
- `grep -c "liam-git-workflow-remote-diagnose" skills/liam-git-workflow-bootstrap/SKILL.md` 输出 ≥ 1 (技能清单中的诊断技能)
- `grep -c "liam-git-workflow " skills/liam-git-workflow-bootstrap/SKILL.md` 输出 ≥ 2 (至少 2 处出现 — 技能清单 + 路由优先级/关系)
- 技能清单中 `|` 分隔的表格行数 ≥ 10 (1 行 header + 1 行 separator + 10 行 skill)
  </action>
  <verify>
    <automated>test -f skills/liam-git-workflow-bootstrap/SKILL.md && grep -c '1%' skills/liam-git-workflow-bootstrap/SKILL.md && grep -c 'liam-git-workflow-create-branch' skills/liam-git-workflow-bootstrap/SKILL.md && grep -c 'liam-git-workflow-remote-diagnose' skills/liam-git-workflow-bootstrap/SKILL.md && echo "PASS: bootstrap SKILL.md exists with all required content"</automated>
  </verify>
  <done>skills/liam-git-workflow-bootstrap/SKILL.md 已创建。包含: frontmatter (name + description), 技能说明, 1% 规则, 完整 10 技能清单表, 路由优先级规则, 5+ 条 Red Flags 禁止借口, 4 个使用示例, 与 liam-git-workflow 主路由的关系说明。所有技能名称与现有 skills/ 目录精确匹配。</done>
</task>

<task type="auto">
  <name>Task 2: 更新 hooks.json SessionStart 引用 bootstrap 技能</name>
  <files>hooks/hooks.json (MODIFY)</files>
  <read_first>
hooks/hooks.json (当前版本 — 13 行)
skills/liam-git-workflow-bootstrap/SKILL.md (Task 1 产物 — 确认技能已存在)
  </read_first>
  <action>
修改 `hooks/hooks.json` 中 SessionStart 钩子的 `additionalContext` 文本。

### 当前值

```
"Git workflow policies loaded. Use Skill tool to discover available skills."
```

### 新值

增加对 bootstrap 技能的具体指引。Agent 需要被告知可以通过 Skill tool 调用 bootstrap 技能来列出所有可用技能:

```json
{
  "hookSpecificOutput": {
    "hookEventName": "SessionStart",
    "additionalContext": "Git workflow skills available. Use Skill tool to invoke 'liam-git-workflow-bootstrap' to discover all 10 Git workflow skills (branch, commit, sync, hotfix, release, diagnose, etc.), or 'liam-git-workflow' as the main natural-language entry point."
  }
}
```

### 修改方式

编辑 `hooks/hooks.json` 文件第 9 行中的 `additionalContext` 字符串。保持 JSON 结构和 PowerShell 命令其余部分不变。确保:

1. JSON 双引号转义正确 (`\"`)
2. PowerShell 字符串内单引号不破坏 quoting
3. 命令格式与原有完全一致 (`powershell -NoProfile -ExecutionPolicy Bypass -Command "..."`)

### 新文案须包含的关键信息

- 提及 `liam-git-workflow-bootstrap` 技能名称 (agent 知道具体调用哪个)
- 提及 `liam-git-workflow` 主入口 (agent 知道备选路径)
- 告知有 10 个可用技能 (agent 知道规模)
- 保留简洁风格 (不宜超过 2-3 句)

### 验证

- `grep -c 'liam-git-workflow-bootstrap' hooks/hooks.json` 输出 1 (bootstrap 技能名称出现)
- `grep -c 'SessionStart' hooks/hooks.json` 输出 1 (SessionStart 钩子仍然存在)
- `grep -c '"additionalContext"' hooks/hooks.json` 输出 1 (additionalContext 字段仍未存在)
- JSON 语法有效: `node -e "JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8'))"` 不报错
  </action>
  <verify>
    <automated>node -e "const j=JSON.parse(require('fs').readFileSync('hooks/hooks.json','utf8')); const ctx=j.hooks.SessionStart[0].hooks[0]; console.assert(ctx.command.includes('liam-git-workflow-bootstrap'), 'bootstrap referenced'); console.assert(ctx.command.includes('liam-git-workflow'), 'main entry referenced'); console.log('PASS: hooks.json valid, bootstrap reference present')"</automated>
  </verify>
  <done>hooks/hooks.json 的 SessionStart additionalContext 已更新。新文案引用 liam-git-workflow-bootstrap 作为技能发现入口，引用 liam-git-workflow 作为自然语言主入口，提及 10 个可用技能。JSON 语法有效，PowerShell 命令结构保持不变。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hooks.json additionalContext → Agent 上下文 | SessionStart 注入的文本影响 Agent 行为决策；恶意文本可能误导 Agent 执行危险操作 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | hooks.json additionalContext 引导文案 | accept | 引导文案仅建议 Agent 使用 Skill tool，不包含可执行的 Git 命令。误导风险极低 — hooks.json 通过 git 版本控制，修改需经过 commit。文件级篡改在本地开发工具威胁模型中为 accept (与 install.ps1、hook 脚本属于同一信任域)。 |
| T-05-02 | Tampering | skills/liam-git-workflow-bootstrap/SKILL.md 技能清单 | accept | 技能清单列出已知技能名称，不含可执行命令。篡改此文件最多导致 agent 路由到错误的技能 (错误技能本身也是安全技能)。文件通过 git 版本控制管理。 |
| T-05-03 | Information Disclosure | bootstrap SKILL.md 内容 | accept | bootstrap 仅包含公开的技能列表和路由规则，无敏感信息 (密钥、令牌、路径、凭证、PII)。 |
</threat_model>

<verification>
验证 bootstrap 技能可用性和 hooks 正确性:

1. `skills/liam-git-workflow-bootstrap/SKILL.md` 存在且包含 frontmatter (name + description)
2. Bootstrap SKILL.md 中的技能名称与 `skills/` 目录下的 10 个目录名精确匹配 (可用 grep 验证)
3. Bootstrap SKILL.md 包含 "1% 规则" 节
4. Bootstrap SKILL.md 包含 Red Flags 表 (至少 5 条禁止借口)
5. Bootstrap SKILL.md 包含 3+ 个使用示例
6. Bootstrap SKILL.md 提及与 liam-git-workflow 主路由的关系
7. `hooks/hooks.json` JSON 语法有效
8. `hooks/hooks.json` additionalContext 引用 `liam-git-workflow-bootstrap`
9. `hooks/hooks.json` additionalContext 引用 `liam-git-workflow` 主入口
10. 两个文件均不包含任何敏感信息 (密钥、令牌、路径)
</verification>

<success_criteria>
- Agent 通过 Skill tool 调用 `liam-git-workflow-bootstrap` 后能获得完整 10 个技能的清单和触发条件
- 会话启动时 hooks 注入的上下文明确引导 Agent 使用 bootstrap 技能
- Bootstrap 技能清单与 `liam-git-workflow` 主路由触发条件保持一致 (无分歧)
- `liam-git-workflow` 主路由未被修改 — bootstrap 作为补充层而非替代
- 现有 10 个技能全部未修改
</success_criteria>

<output>
After completion, create `.planning/phases/05-meta-skill-bootstrap/05-01-SUMMARY.md`
</output>
