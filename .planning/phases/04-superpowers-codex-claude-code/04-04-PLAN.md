---
phase: 04-superpowers-codex-claude-code
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "README.md 安装说明反映新的 symlink + plugin manifest 安装模型"
    - "README.md 不再描述 claude/commands/ 中的 slash commands"
    - "README.md 目录结构反映 Phase 04 后的实际结构 (含 .claude-plugin/)"
    - "CHANGELOG.md 记录了 v0.3.0 的变更"
  artifacts:
    - path: "README.md"
      provides: "用户安装和使用文档 (v0.3.0)"
      contains: "/plugin install"
    - path: "CHANGELOG.md"
      provides: "v0.3.0 变更日志"
      contains: "## 0.3.0"
  key_links:
    - from: "README.md 安装章节"
      to: ".claude-plugin/plugin.json + scripts/install.ps1"
      via: "用户阅读 → 执行命令 → 安装完成"
---

<objective>
更新 README.md 的安装章节和使用说明以反映 Phase 04 插件化安装模型，更新 CHANGELOG.md 记录 v0.3.0 变更。

Purpose: 让用户能根据 README 完成插件安装，了解新的使用方式；CHANGELOG 记录所有变更供维护者追溯。
Output: README.md (重写安装+使用+结构章节), CHANGELOG.md (添加 v0.3.0 条目)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-superpowers-codex-claude-code/04-CONTEXT.md

<interfaces>
<!-- Current README sections that need updating -->
Current README structure (to be modified):
- 运行时支持: "Claude Code：通过自定义 slash commands 安装" → 改为 "Claude Code：通过插件机制安装"
- 在 Claude Code 中使用: "/liam-git-workflow:..." → 改为 "通过 Skill tool 调用 liam-git-workflow 等技能"
- 安装: 整节重写 (文件复制模型 → symlink + plugin 模型)
- 更新: 重写 (无需重新运行 install.ps1，git pull 即生效)
- 安装后的目录策略: 整节重写或删除 (symlink 模型不需要 support 目录)
- 目录结构: 添加 .claude-plugin/，移除 claude/commands/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重写 README.md — 安装、使用、目录结构章节</name>
  <files>README.md (MODIFY — significant rewrite of specific sections)</files>
  <read_first>
README.md (current full version — 193 行)
.claude-plugin/plugin.json (确认存在 — Plan 01 产物)
scripts/install.ps1 (确认已重写 — Plan 03 产物)
.codex-plugin/plugin.json (.codex-plugin/plugin.json 确认版本 0.3.0 — Plan 02 产物)
VERSION (确认 0.3.0 — Plan 02 产物)
  </read_first>
  <action>
修改 `README.md`，替换以下具体段落/章节。**不改动**未列出的段落（如 目标、提交校验、规则来源、当前范围 等保持不变）。

### 1. 替换 "运行时支持" 节（当前第 20-29 行）

旧内容:
```
当前版本支持两个生态：
- Codex：通过本地插件元数据和全局 skills 安装
- Claude Code：通过自定义 slash commands 安装
...
```

替换为:
```markdown
## 运行时支持

当前版本支持两个生态：

- **Claude Code**：通过 `.claude-plugin/plugin.json` 作为标准插件安装，skills 通过 Skill tool 自动可用
- **Codex**：通过 `.codex-plugin/plugin.json` 和市场注册或 symlink 脚本安装

不包含：

- 官方 marketplace 上架流程
- 自动执行高风险 Git 命令的命令包装器
- 面向 Cursor、Copilot、Gemini、OpenCode 的适配层
```

### 2. 替换 "在 Codex 中使用" 节（当前第 33-45 行）— 保持 $trigger 格式不变，仅更新描述文字

在 Codex 技能列表前添加一句说明: "Codex 安装后，以下技能可通过 Skill tool 调用:"

保留所有 `$liam-git-workflow*` 触发词列表。

在末尾添加 `$liam-git-workflow-remote-diagnose`（如果列表中尚未包含 — 当前 10 个技能需要有完整列表）。

### 3. 完全替换 "在 Claude Code 中使用" 节（当前第 47-59 行）

旧内容使用 `/liam-git-workflow:...` slash commands 格式。

替换为:
```markdown
## 在 Claude Code 中使用

插件安装后，所有技能通过 Skill tool 自动可用。在会话中使用自然语言描述 Git 操作需求即可。

主要技能入口:
- `liam-git-workflow` — 默认路由入口
- `liam-git-workflow-help` — 查看所有可用技能
- `liam-git-workflow-create-branch` — 创建分支
- `liam-git-workflow-commit` — 生成中文 Conventional Commit
- `liam-git-workflow-sync-branch` — 同步分支
- `liam-git-workflow-finish` — 完成分支收尾
- `liam-git-workflow-hotfix` — 生产 hotfix 流程
- `liam-git-workflow-release` — 发布流程
- `liam-git-workflow-sync-policy` — 审计 Git 配置与策略对齐
- `liam-git-workflow-remote-diagnose` — 远程操作失败诊断
```

### 4. 完全替换 "安装" 节（当前第 61-86 行）— 整节重写

旧内容描述 `install.ps1` 文件复制模型。

替换为:
```markdown
## 安装

### 前置条件

- Windows 11, PowerShell 5.1+, Git 2.40+
- Claude Code (如需 Claude Code 集成) 或 Codex (如需 Codex 集成)

### Claude Code 安装

**方式 1: /plugin install (推荐)**

在 Claude Code 会话中运行:
```text
/plugin install <仓库路径>
```

Claude Code 会自动发现 `.claude-plugin/plugin.json`，加载 skills/ 目录中的全部技能，并注册 hooks/ 中的钩子。

**方式 2: 启动参数**

```powershell
claude --plugin-dir <仓库路径>
```

安装后，skills/ 中的技能通过 Skill tool 自动可用。无需额外配置。

### Codex 安装

**方式 1: 运行安装脚本 (推荐)**

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\install.ps1 -SkipClaude
```

脚本会在 `~/.agents/skills/liam-git-workflow` 创建指向 `skills/` 的目录符号链接 (directory junction)。Codex 启动时自动扫描该路径。

**方式 2: 添加市场源**

将仓库的 `.agents/plugins/marketplace.json` 添加到 Codex 的市场源列表中，然后在 Codex 插件界面安装 "liam-git-workflow"。

### 同时安装两个平台

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\install.ps1
```

不加 `-SkipCodex` 或 `-SkipClaude` 标志时，脚本会同时配置 Codex (symlink) 和提示 Claude Code 安装命令。

### 从旧版本迁移

如果你之前使用过 0.2.0 或更早版本（文件复制模型），运行带清理标志的安装命令:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\install.ps1 -CleanLegacy
```

`-CleanLegacy` 会清理以下旧安装产物:
- `~/.codex/skills/liam-git-workflow*` (旧 Codex 文件复制)
- `~/.claude/commands/liam-git-workflow/` (旧 Claude Code slash commands)
- 已失效的旧 directory junction
```

### 5. 完全替换 "更新" 节（当前第 89-102 行）

旧内容描述 `update.ps1` 重新运行 install.ps1 的模型。

替换为:
```markdown
## 更新

symlink 安装模式下，更新仓库即更新插件:

```bash
cd <仓库路径>
git pull --rebase
```

无需重新运行安装脚本 — symlink 直接指向仓库文件，git pull 后立即生效。

如果你需要从 `main` 拉取最新版本后重新执行安装:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\update.ps1 -PullLatest
```

`-PullLatest` 会在仓库存在本地未提交修改时拒绝执行，避免误覆盖工作区。
```

### 6. 完全替换 "安装后的目录策略" 节（当前第 146-158 行）

旧内容描述 `liam-git-workflow-support` 目录和路径重写。

替换为:
```markdown
## 安装后的目录策略

所有技能文件通过 symlink (Codex) 或插件目录注册 (Claude Code) 直接指向仓库中的 `skills/` 目录。skill 内部引用 (`../../references/`、`../../scripts/`) 使用仓库内相对路径，在两种安装模式下均正确解析。

这意味着:
- 无需复制文件到平台特定目录
- 无需在安装时重写 skill 内部路径
- `git pull` 后技能内容自动更新
- 同一仓库可同时服务于 Codex 和 Claude Code
```

### 7. 替换 "目录结构" 节（当前第 132-144 行）

旧内容:
```text
.agents/plugins/
.codex-plugin/
claude/commands/
references/
scripts/
skills/
tests/
CHANGELOG.md
VERSION
```

替换为:
```text
.claude-plugin/
.codex-plugin/
.agents/plugins/
skills/
hooks/
references/
scripts/
tests/
CHANGELOG.md
README.md
VERSION
```

写入后验证 Markdown 无明显的格式错误（检查标题 # 对齐、代码块 ``` 配对）。
  </action>
  <acceptance_criteria>
- `grep -c '/plugin install' README.md` 输出 ≥ 1 (Claude Code 安装方式 1)
- `grep -c 'Skill tool' README.md` 输出 ≥ 2 (Claude Code 使用方式)
- `grep -c 'mklink\|symlink\|directory junction\|符号链接' README.md` 输出 ≥ 1 (symlink 安装说明)
- `grep -c 'CleanLegacy' README.md` 输出 ≥ 1 (旧版迁移说明)
- `grep -c '/liam-git-workflow:' README.md` 输出 0 (slash commands 格式已移除)
- `grep -c 'claude/commands' README.md` 输出 0 (废弃目录不再出现于仓库结构)
- `grep -c '\.claude-plugin' README.md` 输出 ≥ 2 (目录结构 + 安装说明)
- `grep -c '文件复制\|Copy-Item\|Copy-DirectoryTree' README.md` 输出 0 (旧安装模型描述已移除)
- `grep -c 'liam-git-workflow-support' README.md` 输出 0 (旧 support 目录策略已移除)
- `grep -c 'git pull' README.md` 输出 ≥ 1 (更新策略: git pull 即更新)
- `grep -c 'liam-git-workflow-remote-diagnose' README.md` 输出 ≥ 2 (诊断技能在 Codex + Claude Code 列表中都出现)
  </acceptance_criteria>
  <verify>
    <automated>grep -c '/plugin install' README.md && grep -c 'Skill tool' README.md && grep -c 'CleanLegacy' README.md && grep -c '\.claude-plugin' README.md && echo "PASS: README.md key patterns present"</automated>
  </verify>
  <done>README.md 已更新反映 v0.3.0 插件化安装模型。安装章节描述 symlink + plugin manifest 方式，使用章节描述 Skill tool 调用，更新章节描述 git pull 即可生效，目录结构反映 Phase 04 实际结构。</done>
</task>

<task type="auto">
  <name>Task 2: 更新 CHANGELOG.md — 添加 v0.3.0 条目</name>
  <files>CHANGELOG.md (MODIFY — prepend new version entry)</files>
  <read_first>
CHANGELOG.md (current full version — 10 行, 仅 0.2.0 条目)
  </read_first>
  <action>
在 `CHANGELOG.md` 文件顶部（`# Changelog` 标题下方，`## 0.2.0` 上方）插入 v0.3.0 条目:

```markdown
## 0.3.0

- 插件化安装: 仓库支持 Claude Code `/plugin install` 标准安装路径
- 新增 `.claude-plugin/plugin.json` — Claude Code 插件注册 manifest
- 新增 `hooks/hooks.json` — SessionStart 会话钩子 (已知限制: Claude Code bug #16538 导致 SessionStart additionalContext 不会注入到 agent 上下文)
- 废弃 `claude/commands/` — 删除 10 个旧 slash commands; Claude Code 侧统一通过 skills 机制加载
- 安装模型从文件复制改为目录符号链接 (Codex `mklink /J` + Claude Code plugin-dir 注册)
- 修正 Codex `marketplace.json` source.path 从 "." 修正为 "../.." (确保 Codex 正确发现 repo root 中的 plugin.json)
- 简化 `scripts/common.ps1` — 移除文件复制和路径重写函数, 更新元数据格式
- 重写 `scripts/install.ps1` — symlink 模型, 支持 `-CleanLegacy` 清理旧安装产物
- 更新 `scripts/link_codex_global_skills.ps1` 和 `scripts/update.ps1` 兼容新安装模型
- 更新 README.md 安装/使用/更新/目录章节反映插件化架构
```

每个条目对应一个 Phase 04 交付物或决策实施。条目顺序按影响力从大到小排列（插件化 → manifest → 废弃 → 重写 → 修正 → 文档）。

保持现有 `## 0.2.0` 条目不变在 v0.3.0 条目之后。
  </action>
  <acceptance_criteria>
- `grep -c '## 0.3.0' CHANGELOG.md` 输出 1
- `grep -c '## 0.3.0' CHANGELOG.md` 在文件中的行号 < `grep -n '## 0.2.0' CHANGELOG.md` 的行号 (0.3.0 在 0.2.0 之前)
- `grep -c 'plugin install' CHANGELOG.md` 输出 ≥ 1
- `grep -c 'claude/commands' CHANGELOG.md` 输出 ≥ 1
- `grep -c '\.claude-plugin' CHANGELOG.md` 输出 ≥ 1
- `grep -c 'hooks/hooks.json' CHANGELOG.md` 输出 ≥ 1
- `grep -c 'marketplace.json' CHANGELOG.md` 输出 ≥ 1
- `grep -c 'symlink\|符号链接\|mklink' CHANGELOG.md` 输出 ≥ 1
- `grep -c 'CleanLegacy' CHANGELOG.md` 输出 ≥ 1
- `## 0.2.0` 条目仍然存在且内容完整
  </acceptance_criteria>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('CHANGELOG.md','utf8'); const v03=c.indexOf('## 0.3.0'); const v02=c.indexOf('## 0.2.0'); console.assert(v03>=0,'0.3.0 entry exists'); console.assert(v02>=0,'0.2.0 entry exists'); console.assert(v03<v02,'0.3.0 appears before 0.2.0'); console.log('PASS: CHANGELOG.md order correct')"</automated>
  </verify>
  <done>CHANGELOG.md 已添加 v0.3.0 条目，记录所有 Phase 04 变更，条目按影响力排序，v0.2.0 条目保留不变。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md 安装命令 → 用户终端 | 用户复制 README 中的命令到终端执行；恶意修改 README 可引导用户执行危险命令 |
| CHANGELOG.md → 用户信任 | CHANGELOG 记录的变更描述影响用户对版本的行为预期 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Tampering | README.md 安装指令 | accept | README 中的安装命令使用标准 PowerShell 模式 (`-ExecutionPolicy Bypass -File .\scripts\install.ps1`)，无 `-EncodedCommand` 或混淆内容。文件通过 git 版本控制，修改需经过 commit。用户应检查 git log 确认 README 未被篡改。文档级篡改在本地开发工具威胁模型中为 accept (与 install.ps1 属于同一信任域)。 |
| T-04-18 | Information Disclosure | README.md 和 CHANGELOG.md 内容 | accept | 两个文件均不包含任何敏感信息（密钥、令牌、路径、凭证、PII）。文档内容完全公开。 |
</threat_model>

<verification>
验证文档完整性:
1. README.md 包含 "/plugin install" (Claude Code 推荐安装方式)
2. README.md 包含 "CleanLegacy" (旧版迁移路径)
3. README.md 不再包含 "/liam-git-workflow:" slash command 格式
4. README.md 不再包含 "claude/commands" 目录引用
5. README.md 目录结构包含 .claude-plugin/
6. CHANGELOG.md 包含 "## 0.3.0" 条目且在 "## 0.2.0" 之前
7. CHANGELOG.md 0.3.0 条目覆盖所有 Phase 04 关键变更
</verification>

<success_criteria>
- 新用户能仅凭 README.md 完成 Claudia Code + Codex 双平台安装
- 旧用户能从 README.md 找到 -CleanLegacy 迁移路径
- CHANGELOG.md 提供 v0.3.0 完整变更追溯
- README 中不再有任何过时的 slash command 或文件复制安装描述
</success_criteria>

<output>
After completion, create `.planning/phases/04-superpowers-codex-claude-code/04-04-SUMMARY.md`
</output>
