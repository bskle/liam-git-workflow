---
phase: 04-superpowers-codex-claude-code
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .codex-plugin/plugin.json
  - .agents/plugins/marketplace.json
  - VERSION
autonomous: true
requirements: []

must_haves:
  truths:
    - ".codex-plugin/plugin.json 版本号已更新至 0.3.0"
    - ".agents/plugins/marketplace.json 的 source.path 正确指向仓库根目录"
    - "VERSION 文件内容为 0.3.0"
    - "Codex 市场安装路径能正确发现 .codex-plugin/plugin.json 和 skills/"
  artifacts:
    - path: ".codex-plugin/plugin.json"
      provides: "Codex 插件元数据 (v0.3.0)"
      contains: "0.3.0"
    - path: ".agents/plugins/marketplace.json"
      provides: "Codex 市场注册 (修正 source.path)"
      contains: "\"../..\""
    - path: "VERSION"
      provides: "仓库版本标识"
      contains: "0.3.0"
  key_links:
    - from: "Codex 市场扫描"
      to: ".codex-plugin/plugin.json"
      via: "marketplace.json source.path 解析到仓库根目录"
    - from: ".codex-plugin/plugin.json skills 字段"
      to: "skills/ 目录"
      via: "Codex 插件加载器 (skills 路径声明)"
---

<objective>
更新 Codex 侧插件配置: 将 .codex-plugin/plugin.json 版本提升至 0.3.0, 修复 marketplace.json 的 source.path 路径解析错误, 将 VERSION 文件更新至 0.3.0。

Purpose: 确保 Codex 市场安装和市场外 symlink 安装两种路径都能正确发现插件及其 skills。
Output: .codex-plugin/plugin.json (更新), .agents/plugins/marketplace.json (修正), VERSION (更新)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-superpowers-codex-claude-code/04-CONTEXT.md
@.planning/phases/04-superpowers-codex-claude-code/04-RESEARCH.md

<interfaces>
<!-- Key context for marketplace.json source.path resolution -->
From RESEARCH.md Pitfall 5:
- marketplace.json is at `liam-git-workflow/.agents/plugins/marketplace.json`
- Current source.path "." resolves to `.agents/plugins/` — NOT the repo root
- To point to the repo root (where .codex-plugin/plugin.json and skills/ live), use "../.." (up two levels)
- Codex expects to find plugin.json from the resolved source.path

From RESEARCH.md Code Examples:
- Exact fixed marketplace.json provided in "Codex marketplace.json (fixed source.path)" section
- Keep existing structure and fields, only change `"path": "."` to `"path": "../.."`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 .codex-plugin/plugin.json — 版本提升至 0.3.0</name>
  <files>.codex-plugin/plugin.json (MODIFY — existing file, version and description fields only)</files>
  <read_first>
.codex-plugin/plugin.json (current state — read fully before modifying)
VERSION (reference: target version 0.3.0, but note this file will be updated by Task 3)
  </read_first>
  <action>
修改 `.codex-plugin/plugin.json`，做以下两处变更:

1. 将 `"version"` 字段值从 `"0.2.0"` 改为 `"0.3.0"`
2. 将 `"description"` 字段值从 `"Personal Git workflow plugin for Codex with a shared cross-runtime package for Codex skills, Claude commands, install, and update flows."` 改为 `"面向 Codex 和 Claude Code 的个人 Git 工作流插件包 — 分支管理、提交规范、同步策略、远程诊断"`

**不修改** 其他任何字段（name, author, license, skills, interface 全部保持不变）。
**不添加** 任何新字段。

当前完整文件结构（仅展示需修改的字段位置）:
```json
{
  "name": "liam-git-workflow",
  "version": "0.3.0",           // ← 从 "0.2.0" 改为 "0.3.0"
  "description": "面向 Codex 和 Claude Code 的个人 Git 工作流插件包 — 分支管理、提交规范、同步策略、远程诊断",  // ← 新中文描述
  "author": { "name": "Liam" },  // ← 不变
  "license": "UNLICENSED",       // ← 不变
  "skills": "./skills/",         // ← 不变
  "interface": { ... }           // ← 全部不变
}
```

写入后验证 JSON 有效性: `node -e "JSON.parse(require('fs').readFileSync('.codex-plugin/plugin.json','utf8'))"`
  </action>
  <acceptance_criteria>
- `grep '"version"' .codex-plugin/plugin.json` 输出包含 "0.3.0"
- `grep '"version"' .codex-plugin/plugin.json` 输出不包含 "0.2.0"
- `grep '"description"' .codex-plugin/plugin.json` 输出包含 "面向 Codex 和 Claude Code 的个人 Git 工作流插件包"
- `grep -c '"name"' .codex-plugin/plugin.json` 输出 1，值为 "liam-git-workflow" (未变)
- `grep -c '"skills"' .codex-plugin/plugin.json` 输出 1，值为 "./skills/" (未变)
- `grep -c '"license"' .codex-plugin/plugin.json` 输出 1，值为 "UNLICENSED" (未变)
- JSON 解析成功无错误
  </acceptance_criteria>
  <verify>
    <automated>node -e "const j=JSON.parse(require('fs').readFileSync('.codex-plugin/plugin.json','utf8')); console.assert(j.version==='0.3.0','version must be 0.3.0'); console.assert(j.name==='liam-git-workflow','name unchanged'); console.assert(j.skills==='./skills/','skills unchanged'); console.log('PASS: .codex-plugin/plugin.json updated')"</automated>
  </verify>
  <done>.codex-plugin/plugin.json 版本更新至 0.3.0，描述更新为中文，其他所有字段保持不变。</done>
</task>

<task type="auto">
  <name>Task 2: 修复 .agents/plugins/marketplace.json — source.path 路径修正</name>
  <files>.agents/plugins/marketplace.json (MODIFY — existing file, one field change)</files>
  <read_first>
.agents/plugins/marketplace.json (current state — read fully before modifying)
.planning/phases/04-superpowers-codex-claude-code/04-RESEARCH.md (Pitfall 5: "Marketplace source.path resolution ambiguity" + Code Examples: "Codex marketplace.json (fixed source.path)")
  </read_first>
  <action>
修改 `.agents/plugins/marketplace.json`，仅改一处：

将 `"source"` 对象中的 `"path"` 字段值从 `"."` 改为 `"../.."`:

当前值:
```json
"source": {
  "source": "local",
  "path": "."
}
```

修改为:
```json
"source": {
  "source": "local",
  "path": "../.."
}
```

路径解析逻辑（per RESEARCH.md + openai/codex plugin-creator 文档）:
- marketplace.json 位于 `liam-git-workflow/.agents/plugins/marketplace.json`
- `source.path` 相对于 marketplace.json 所在目录解析
- 当前 "." → 解析到 `.agents/plugins/` → Codex 找不到 plugin.json/skills/ (它们都在 repo root)
- 修正 ".." → 解析到 `.agents/` → 仍然不对
- 修正 "../.." → 解析到 repo root `liam-git-workflow/` → Codex 正确发现 `.codex-plugin/plugin.json` 和 `skills/`

**不修改** 其他任何字段（name, interface, plugins[0].name, source.source, policy, category 全部保持不变）。

写入后验证 JSON 有效性: `node -e "JSON.parse(require('fs').readFileSync('.agents/plugins/marketplace.json','utf8'))"`
  </action>
  <acceptance_criteria>
- `grep '"path"' .agents/plugins/marketplace.json` 输出包含 "../.."
- `grep '"path"' .agents/plugins/marketplace.json` 输出不包含 "\".\"" (单独的点)
- `grep '"source": "local"' .agents/plugins/marketplace.json` 输出存在 (未变)
- `grep '"name": "liam-git-workflow"' .agents/plugins/marketplace.json` 输出存在 (未变)
- JSON 解析成功无错误
  </acceptance_criteria>
  <verify>
    <automated>node -e "const j=JSON.parse(require('fs').readFileSync('.agents/plugins/marketplace.json','utf8')); console.assert(j.plugins[0].source.path==='../..','path must be ../..'); console.assert(j.plugins[0].name==='liam-git-workflow','plugin name unchanged'); console.log('PASS: marketplace.json source.path fixed')"</automated>
  </verify>
  <done>.agents/plugins/marketplace.json 的 source.path 已从 "." 修正为 "../.."，Codex 市场扫描现可正确解析到仓库根目录并发现 .codex-plugin/plugin.json 和 skills/。</done>
</task>

<task type="auto">
  <name>Task 3: 更新 VERSION 文件至 0.3.0</name>
  <files>VERSION (MODIFY — existing file, single line)</files>
  <read_first>
VERSION (current content: "0.2.0")
  </read_first>
  <action>
将 `VERSION` 文件内容从 `0.2.0` 改为 `0.3.0`（单行，无换行符多余空格）。

这是新插件化版本的标识号。版本号选择 0.3.0 的理由:
- 0.1.0: 初始版本 (Codex-only skills)
- 0.2.0: 双运行时安装 (Codex + Claude Code, 文件复制模型)
- 0.3.0: 插件化安装 (symlink + plugin manifest 模型, 本 Phase 04 产物)

文件应只包含一行: `0.3.0`（无尾随空格或额外换行）。
  </action>
  <acceptance_criteria>
- `cat VERSION` 输出 "0.3.0" (精确匹配，无多余空格或换行)
- `wc -l VERSION` 输出 1 (单行) 或文件大小约为 5 字节
  </acceptance_criteria>
  <verify>
    <automated>V=$(cat VERSION); test "$V" = "0.3.0" && echo "PASS: VERSION=0.3.0" || (echo "FAIL: VERSION=$V expected 0.3.0" && exit 1)</automated>
  </verify>
  <done>VERSION 文件已更新为 0.3.0，与 .claude-plugin/plugin.json 和 .codex-plugin/plugin.json 版本号一致。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| marketplace.json → Codex 插件发现 | Codex 读取 marketplace.json 以定位插件源。source.path 指向本地仓库路径，间接影响哪些文件被加载为插件内容 |
| .codex-plugin/plugin.json → Codex 插件加载器 | Codex 根据 plugin.json 的 skills 字段路径加载技能。路径错误会导致技能不可用但不造成安全影响 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | marketplace.json source.path | accept | source.path 值为静态 "../.." 指向仓库根目录，不接收用户输入或环境变量。篡改需要本地文件系统写入权限。 |
| T-04-08 | Spoofing | .codex-plugin/plugin.json name | accept | name 字段 "liam-git-workflow" 用于本地识别，非安全关键。Codex 市场认证在 ON_INSTALL 时触发（policy.authentication: "ON_INSTALL"），但最终认证机制由 Codex 平台提供。 |
| T-04-09 | Information Disclosure | VERSION + plugin.json metadata | accept | 所有文件仅包含非敏感元数据（版本号、描述文本、作者名）。无凭证、无 PII、无内部路径信息。 |
| T-04-10 | Repudiation | marketplace.json plugin entry | accept | marketplace.json 声明本地插件源（source: "local"），不涉及远程下载或自动更新。安装操作记录在 Codex 自身日志中，不由仓库控制。 |
</threat_model>

<verification>
验证 Codex 侧配置完整性:
1. `.codex-plugin/plugin.json` 版本为 0.3.0
2. `.agents/plugins/marketplace.json` source.path 为 "../.."
3. `VERSION` 为 0.3.0
4. 三个文件版本号一致
</verification>

<success_criteria>
- Codex 市场安装路径能正确从 marketplace.json 解析到仓库根目录
- .codex-plugin/plugin.json 版本号与 VERSION 文件一致
- 所有三个文件的 JSON 均有效且字段正确
</success_criteria>

<output>
After completion, create `.planning/phases/04-superpowers-codex-claude-code/04-02-SUMMARY.md`
</output>
