---
phase: 04-superpowers-codex-claude-code
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/install.ps1
  - scripts/common.ps1
  - scripts/link_codex_global_skills.ps1
  - scripts/update.ps1
autonomous: true
requirements: []

must_haves:
  truths:
    - "install.ps1 创建目录符号链接 (mklink /J) 而非复制文件"
    - "install.ps1 支持 -CleanLegacy 标志清理旧文件复制安装产物"
    - "install.ps1 同时支持 Codex symlink 和 Claude Code plugin-dir 两条安装路径"
    - "common.ps1 不再包含文件复制函数 (Install-LiamGitWorkflowCodex, Install-LiamGitWorkflowClaude, Copy-DirectoryTree, Copy-FileWithReplacements)"
    - "link_codex_global_skills.ps1 和 update.ps1 与新 install.ps1 兼容"
  artifacts:
    - path: "scripts/install.ps1"
      provides: "symlink 模型安装脚本 (D-07)"
      min_lines: 80
    - path: "scripts/common.ps1"
      provides: "共享工具函数 (不含文件复制逻辑)"
      max_lines: 70
    - path: "scripts/link_codex_global_skills.ps1"
      provides: "Codex-only 安装快捷入口"
    - path: "scripts/update.ps1"
      provides: "更新脚本"
  key_links:
    - from: "install.ps1 -SkipClaude"
      to: "~/.agents/skills/liam-git-workflow (directory junction → repo/skills/)"
      via: "mklink /J"
    - from: "install.ps1"
      to: "Claude Code /plugin install or --plugin-dir"
      via: "Write-Host 提示信息"
    - from: "common.ps1 helper functions"
      to: "install.ps1 / update.ps1"
      via: "dot-source (Join-Path $PSScriptRoot 'common.ps1')"
---

<objective>
将 install.ps1 从文件复制模型重写为 symlink + plugin manifest 模型，简化 common.ps1 移除文件复制函数，更新 link_codex_global_skills.ps1 和 update.ps1 以兼容新安装模型。

Purpose: 实现 D-07 (symlink 安装脚本)，消除 D-03 明确禁止的路径重写行为，让仓库更新 (git pull) 立即生效 (symlink 而非复制)。
Output: scripts/install.ps1 (重写), scripts/common.ps1 (简化), scripts/link_codex_global_skills.ps1 (更新), scripts/update.ps1 (更新)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-superpowers-codex-claude-code/04-CONTEXT.md
@.planning/phases/04-superpowers-codex-claude-code/04-RESEARCH.md

<interfaces>
<!-- Current script signatures that remain valid -->
From scripts/common.ps1 (functions to KEEP):
- Resolve-LiamGitWorkflowRepoRoot -RepoRoot -ScriptRoot → resolved root path
- Get-LiamGitWorkflowVersion -RepoRoot → version string
- Ensure-Directory -Path (creates directory if missing)
- Reset-Path -Path (removes path if exists)

From scripts/common.ps1 (functions to REMOVE):
- Copy-DirectoryTree -Source -Destination (file-copy, replaced by symlink)
- Copy-FileWithReplacements -Source -Destination -Replacements (path rewriting, banned by D-03)
- Convert-ToForwardSlashPath -Path (used only by file-copy logic)
- Install-LiamGitWorkflowCodex -RepoRoot -CodexHome (replaced by symlink)
- Install-LiamGitWorkflowClaude -RepoRoot -ClaudeHome -Version (replaced by plugin-dir)

From scripts/common.ps1 (function to REWRITE):
- Write-LiamGitWorkflowInstallMetadata (keep, but update payload format)

From research, Codex bug #17344: file-level symlinks to SKILL.md are dropped; directory junctions (mklink /J) work correctly.

From research template for install.ps1: see RESEARCH.md "Simplified install.ps1 (symlink model)" code example.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重写 scripts/install.ps1 — symlink 模型安装脚本</name>
  <files>scripts/install.ps1 (REWRITE — complete replacement of existing file)</files>
  <read_first>
scripts/install.ps1 (current version — understand the existing flow before replacing)
scripts/common.ps1 (will remain dot-sourced; need to see which functions are currently called)
.planning/phases/04-superpowers-codex-claude-code/04-RESEARCH.md (sections: "Simplified install.ps1 (symlink model)" code example, Pitfall 3 "Codex symlink bug #17344", Runtime State Inventory for -CleanLegacy design)
  </read_first>
  <action>
用以下完整内容**完全替换** `scripts/install.ps1`（当前 33 行的文件复制模型 → 新 100+ 行的 symlink 模型）:

```powershell
param(
    [string]$RepoRoot,
    [string]$CodexSkillsHome = "$env:USERPROFILE\.agents\skills",
    [switch]$SkipCodex,
    [switch]$SkipClaude,
    [switch]$CleanLegacy
)

$ErrorActionPreference = 'Stop'

# Dot-source shared utilities (Resolve-LiamGitWorkflowRepoRoot, Get-LiamGitWorkflowVersion,
# Ensure-Directory, Reset-Path, Write-LiamGitWorkflowInstallMetadata)
. (Join-Path $PSScriptRoot 'common.ps1')

$resolvedRoot = Resolve-LiamGitWorkflowRepoRoot -RepoRoot $RepoRoot -ScriptRoot $PSScriptRoot
$version = Get-LiamGitWorkflowVersion -RepoRoot $resolvedRoot

# ── Codex: 目录 junction (mklink /J) 从 ~/.agents/skills/liam-git-workflow → repo/skills/ ──
if (-not $SkipCodex) {
    $codexTarget = Join-Path $CodexSkillsHome 'liam-git-workflow'
    $codexSource = Join-Path $resolvedRoot 'skills'

    if (-not (Test-Path -LiteralPath $codexSource)) {
        throw "Skills source directory not found: $codexSource"
    }

    Ensure-Directory -Path $CodexSkillsHome

    # 如果目标已存在（旧 junction 或遗留目录），先删除
    if (Test-Path -LiteralPath $codexTarget) {
        Write-Host "Removing existing install at $codexTarget"
        cmd /c rmdir "$codexTarget" 2>$null
        if (Test-Path -LiteralPath $codexTarget) {
            Remove-Item -LiteralPath $codexTarget -Recurse -Force
        }
    }

    # 使用 cmd /c mklink /J（PowerShell New-Item -ItemType Junction 在 Windows 10+ 也可用，
    # 但 mklink /J 在 PowerShell 中需通过 cmd 调用）
    $mklinkCmd = "mklink /J `"$codexTarget`" `"$codexSource`""
    cmd /c $mklinkCmd
    if ($LASTEXITCODE -ne 0) {
        throw "Failed to create directory junction: $codexTarget -> $codexSource (exit code $LASTEXITCODE)"
    }

    # 验证 junction 创建成功
    $junctionTarget = (Get-Item -LiteralPath $codexTarget -Force).Target
    if (-not $junctionTarget) {
        throw "Junction created but Target property is empty: $codexTarget"
    }
    Write-Host "Codex: symlinked $codexTarget -> $codexSource"
}

# ── Claude Code: 插件目录即为仓库本身; 提示用户使用 /plugin install ──
if (-not $SkipClaude) {
    Write-Host "Claude Code: 仓库已配置为插件目录。使用以下任一方式安装:"
    Write-Host "  方式 1: 在 Claude Code 会话中运行 /plugin install `"$resolvedRoot`""
    Write-Host "  方式 2: 启动时指定插件目录 claude --plugin-dir `"$resolvedRoot`""
    Write-Host ""
    Write-Host "安装后，skills/ 中的技能将通过 Skill tool 自动可用。"
}

# ── CleanLegacy: 清理旧版文件复制安装产物 ──
if ($CleanLegacy) {
    Write-Host "Cleaning legacy install artifacts..."

    # 旧 Codex 文件复制产物: ~/.codex/skills/liam-git-workflow*
    $legacyCodexSkills = Join-Path $env:USERPROFILE '.codex\skills'
    if (Test-Path -LiteralPath $legacyCodexSkills) {
        $legacyDirs = Get-ChildItem -LiteralPath $legacyCodexSkills -Directory -Filter 'liam-git-workflow*' -ErrorAction SilentlyContinue
        foreach ($dir in $legacyDirs) {
            Write-Host "  Removing legacy Codex skills: $($dir.FullName)"
            Remove-Item -LiteralPath $dir.FullName -Recurse -Force
        }
    }

    # 旧 Claude Code 命令产物: ~/.claude/commands/liam-git-workflow/
    $legacyClaudeCommands = Join-Path $env:USERPROFILE '.claude\commands\liam-git-workflow'
    if (Test-Path -LiteralPath $legacyClaudeCommands) {
        Write-Host "  Removing legacy Claude commands: $legacyClaudeCommands"
        Remove-Item -LiteralPath $legacyClaudeCommands -Recurse -Force
    }

    # 旧 Codex symlink: ~/.agents/skills/ 中可能存在的旧 junction
    $legacyAgentsSkills = Join-Path $env:USERPROFILE '.agents\skills\liam-git-workflow'
    if (Test-Path -LiteralPath $legacyAgentsSkills) {
        $item = Get-Item -LiteralPath $legacyAgentsSkills -Force
        if ($item.LinkType -eq 'Junction' -or $item.Attributes -band [System.IO.FileAttributes]::ReparsePoint) {
            Write-Host "  Removing old junction: $legacyAgentsSkills"
            cmd /c rmdir "$legacyAgentsSkills" 2>$null
            if (Test-Path -LiteralPath $legacyAgentsSkills) {
                Remove-Item -LiteralPath $legacyAgentsSkills -Recurse -Force
            }
        }
    }

    Write-Host "Legacy cleanup complete."
}

# ── 写入安装元数据 ──
Write-LiamGitWorkflowInstallMetadata `
    -RepoRoot $resolvedRoot `
    -InstalledCodex (-not $SkipCodex) `
    -InstalledClaude (-not $SkipClaude) `
    -Version $version

Write-Host ""
Write-Host "Liam Git Workflow $version 安装完成"
Write-Host "  Codex: $(if (-not $SkipCodex) { '已安装 (symlink)' } else { '已跳过' })"
Write-Host "  Claude Code: $(if (-not $SkipClaude) { '已配置 (使用 /plugin install 完成安装)' } else { '已跳过' })"
if ($CleanLegacy) {
    Write-Host "  旧安装产物: 已清理"
}
```

关键设计决策（per RESEARCH + D-03, D-07）:
- `mklink /J` (目录 junction) 而非 `mklink` (文件 symlink) — Codex bug #17344: 文件级 symlink 会被跳过
- 不再复制文件或重写 SKILL.md 路径 — D-03 禁止路径重写, symlink 天然保持相对路径正确
- Claude Code 侧不再做文件操作 — 仓库本身即为插件目录 (.claude-plugin/plugin.json 已存在)
- -CleanLegacy 同时清理三种旧产物: ~/.codex/skills/ (文件复制), ~/.claude/commands/ (slash commands), ~/.agents/skills/ (旧 junction)
- -SkipCodex / -SkipClaude 保持与旧版相同的参数名以向后兼容
  </action>
  <acceptance_criteria>
- `grep -c 'mklink /J' scripts/install.ps1` 输出 ≥ 1 (目录 junction 创建)
- `grep -c 'Copy-Item\|Copy-DirectoryTree\|Copy-FileWithReplacements' scripts/install.ps1` 输出 0 (无文件复制)
- `grep -c '\\.Replace(' scripts/install.ps1` 输出 0 (无路径字符串替换)
- `grep -c 'CleanLegacy' scripts/install.ps1` 输出 ≥ 3 (参数声明 + 逻辑 + 提示)
- `grep -c '$SkipCodex' scripts/install.ps1` 输出 ≥ 3
- `grep -c '$SkipClaude' scripts/install.ps1` 输出 ≥ 3
- `grep -c 'Write-LiamGitWorkflowInstallMetadata' scripts/install.ps1` 输出 ≥ 1
- `grep -c '\\.agents\\\\skills' scripts/install.ps1` 输出 ≥ 1 (Codex symlink 目标路径)
- `grep -c '/plugin install' scripts/install.ps1` 输出 ≥ 1 (Claude Code 安装提示)
- 文件行数 ≥ 80 (重写后应有足够内容)
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "& { $errors = Get-Command -Syntax (Join-Path $PSScriptRoot 'scripts/install.ps1') -ErrorAction SilentlyContinue; if (-not $?) { throw 'install.ps1 has syntax errors' } else { Write-Host 'PASS: install.ps1 syntax valid' } }"</automated>
  </verify>
  <done>install.ps1 已完成从文件复制模型到 symlink + plugin manifest 模型的重写。支持 -SkipCodex, -SkipClaude, -CleanLegacy 参数。不再复制文件或重写路径。</done>
</task>

<task type="auto">
  <name>Task 2: 简化 scripts/common.ps1 — 移除文件复制函数，更新元数据写入</name>
  <files>scripts/common.ps1 (MODIFY — remove functions, rewrite one)</files>
  <read_first>
scripts/common.ps1 (current version — 181 lines, contains 8 functions)
scripts/install.ps1 (already rewritten by Task 1 — verify which functions it dot-sources)
  </read_first>
  <action>
修改 `scripts/common.ps1`，执行以下操作:

### 删除以下 5 个函数（完整移除，包括注释）:

1. **Copy-DirectoryTree** (行 43-52) — 文件复制，被 symlink 取代
2. **Copy-FileWithReplacements** (行 54-68) — 路径重写复制，被 D-03 禁止
3. **Convert-ToForwardSlashPath** (行 70-73) — 仅被 Copy-FileWithReplacements 使用
4. **Install-LiamGitWorkflowCodex** (行 76-116) — 旧 Codex 安装逻辑
5. **Install-LiamGitWorkflowClaude** (行 118-153) — 旧 Claude Code 安装逻辑

### 保留以下 5 个函数（不修改，或仅微调注释）:

1. **Set-StrictMode -Version Latest** (行 1) — 保留在文件顶部
2. **Resolve-LiamGitWorkflowRepoRoot** (行 3-14) — 被 install.ps1 和 update.ps1 调用
3. **Get-LiamGitWorkflowVersion** (行 16-25) — 被 install.ps1 调用
4. **Ensure-Directory** (行 27-33) — 被 install.ps1 调用
5. **Reset-Path** (行 35-41) — 保留（仍可用于 future tasks）

### 重写 Write-LiamGitWorkflowInstallMetadata (行 155-180):

将新 install.ps1 调用的参数签名改为 (移除 codexHome/claudeHome，添加简化的 plugin 模式元数据):

```powershell
function Write-LiamGitWorkflowInstallMetadata {
    param(
        [string]$RepoRoot,
        [string]$Version,
        [bool]$InstalledCodex,
        [bool]$InstalledClaude
    )

    $metadataRoot = Join-Path $env:USERPROFILE '.liam-git-workflow'
    Ensure-Directory -Path $metadataRoot

    $payload = [ordered]@{
        version = $Version
        repoRoot = $RepoRoot
        installedCodex = $InstalledCodex
        installedClaude = $InstalledClaude
        installModel = if ($InstalledCodex) { 'symlink' } else { 'plugin-manifest' }
        installedAt = (Get-Date).ToString('o')
    }

    $json = $payload | ConvertTo-Json -Depth 4
    Set-Content -LiteralPath (Join-Path $metadataRoot 'install.json') -Value $json -Encoding UTF8
}
```

对比旧版变更:
- 移除 `$CodexHome` 和 `$ClaudeHome` 参数（不再需要，旧值在新模型中无意义）
- 添加 `installModel` 字段: "symlink" (Codex) 或 "plugin-manifest" (仅 Claude Code)
- 移除 `codexHome` 和 `claudeHome` JSON 字段

写入后验证: `powershell -NoProfile -Command "& { . '.\scripts\common.ps1'; Write-Host 'PASS: common.ps1 dot-sources successfully' }"`
  </action>
  <acceptance_criteria>
- `grep -c 'function Copy-DirectoryTree' scripts/common.ps1` 输出 0 (已删除)
- `grep -c 'function Copy-FileWithReplacements' scripts/common.ps1` 输出 0 (已删除)
- `grep -c 'function Convert-ToForwardSlashPath' scripts/common.ps1` 输出 0 (已删除)
- `grep -c 'function Install-LiamGitWorkflowCodex' scripts/common.ps1` 输出 0 (已删除)
- `grep -c 'function Install-LiamGitWorkflowClaude' scripts/common.ps1` 输出 0 (已删除)
- `grep -c 'function Resolve-LiamGitWorkflowRepoRoot' scripts/common.ps1` 输出 1 (保留)
- `grep -c 'function Get-LiamGitWorkflowVersion' scripts/common.ps1` 输出 1 (保留)
- `grep -c 'function Ensure-Directory' scripts/common.ps1` 输出 1 (保留)
- `grep -c 'function Reset-Path' scripts/common.ps1` 输出 1 (保留)
- `grep -c 'function Write-LiamGitWorkflowInstallMetadata' scripts/common.ps1` 输出 1 (保留，已重写)
- `grep -c 'installModel' scripts/common.ps1` 输出 ≥ 1 (新元数据字段)
- `grep -c 'codexHome\|claudeHome' scripts/common.ps1` 输出 0 (旧元数据字段已移除)
- `grep -c '{{REFERENCES_ROOT}}\|{{SCRIPTS_ROOT}}\|{{REPO_ROOT}}\|{{VERSION}}' scripts/common.ps1` 输出 0 (路径替换模板已移除)
- 文件总行数 ≤ 70
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "& { . '.\scripts\common.ps1' -ErrorAction Stop; $funcs = @('Resolve-LiamGitWorkflowRepoRoot','Get-LiamGitWorkflowVersion','Ensure-Directory','Reset-Path','Write-LiamGitWorkflowInstallMetadata'); foreach ($f in $funcs) { if (-not (Get-Command $f -ErrorAction SilentlyContinue)) { throw \"Missing function: $f\" } }; $removed = @('Copy-DirectoryTree','Copy-FileWithReplacements','Convert-ToForwardSlashPath','Install-LiamGitWorkflowCodex','Install-LiamGitWorkflowClaude'); foreach ($r in $removed) { if (Get-Command $r -ErrorAction SilentlyContinue) { throw \"Should be removed: $r\" } }; Write-Host 'PASS: common.ps1 functions correct' }"</automated>
  </verify>
  <done>common.ps1 已简化：移除了 5 个文件复制/路径重写函数，保留了 4 个工具函数，重写了 Write-LiamGitWorkflowInstallMetadata 以输出插件化安装元数据。文件 ≤ 70 行。</done>
</task>

<task type="auto">
  <name>Task 3: 更新 scripts/link_codex_global_skills.ps1 和 scripts/update.ps1 — 兼容新 install.ps1</name>
  <files>
scripts/link_codex_global_skills.ps1 (MODIFY — update delegation call)
scripts/update.ps1 (MODIFY — update delegation call)
  </files>
  <read_first>
scripts/link_codex_global_skills.ps1 (current version — 20 lines, delegates to install.ps1 -SkipClaude)
scripts/update.ps1 (current version — 55 lines, delegates to install.ps1)
scripts/install.ps1 (already rewritten by Task 1 — verify new parameter names)
  </read_first>
  <action>
### Part A: 更新 scripts/link_codex_global_skills.ps1

用以下内容**完全替换** `scripts/link_codex_global_skills.ps1`:

```powershell
param(
  [string]$RepoRoot,
  [string]$CodexSkillsHome = "$env:USERPROFILE\.agents\skills"
)

$ErrorActionPreference = "Stop"

$installScript = Join-Path $PSScriptRoot "install.ps1"

$installArgs = @(
    '-ExecutionPolicy', 'Bypass',
    '-File', $installScript,
    '-SkipClaude'
)

if ($RepoRoot) {
    $installArgs += '-RepoRoot'
    $installArgs += $RepoRoot
}

if ($CodexSkillsHome -ne "$env:USERPROFILE\.agents\skills") {
    $installArgs += '-CodexSkillsHome'
    $installArgs += $CodexSkillsHome
}

& powershell @installArgs

if ($LASTEXITCODE -ne 0) {
  throw "Codex install failed."
}

Write-Host "Codex skills installed via symlink"
```

变更说明:
- 移除 `-CodexHome` 参数（新 install.ps1 使用 `-CodexSkillsHome` 直接指向 `~/.agents/skills`）
- 添加 `-CodexSkillsHome` 参数以允许自定义 Codex skills 根路径
- 转发 `-SkipClaude` 标志给 install.ps1
- 不再假设 CodexHome 在 `~/.codex`（新模型中 skills 在 `~/.agents/skills/`）

### Part B: 更新 scripts/update.ps1

修改 `scripts/update.ps1`（保留 PullLatest 逻辑，更新委托调用部分）:

将委托调用段的参数从旧格式:
```powershell
$installArgs = @(
    '-ExecutionPolicy', 'Bypass',
    '-File', (Join-Path $PSScriptRoot 'install.ps1'),
    '-RepoRoot', $resolvedRepoRoot,
    '-CodexHome', $CodexHome,
    '-ClaudeHome', $ClaudeHome
)
```

改为新格式:
```powershell
$installArgs = @(
    '-ExecutionPolicy', 'Bypass',
    '-File', (Join-Path $PSScriptRoot 'install.ps1'),
    '-RepoRoot', $resolvedRepoRoot
)
```

同时将 update.ps1 的参数声明中的 `$CodexHome` 和 `$ClaudeHome` 移除（不再被新 install.ps1 使用）:

旧参数:
```powershell
param(
    [string]$RepoRoot,
    [string]$CodexHome = $(if ($env:CODEX_HOME) { $env:CODEX_HOME } else { Join-Path $env:USERPROFILE '.codex' }),
    [string]$ClaudeHome = $env:USERPROFILE,
    [switch]$SkipCodex,
    [switch]$SkipClaude,
    [switch]$PullLatest
)
```

新参数:
```powershell
param(
    [string]$RepoRoot,
    [switch]$SkipCodex,
    [switch]$SkipClaude,
    [switch]$PullLatest
)
```

保留 `-SkipCodex` 和 `-SkipClaude`（如果设置则传入 install.ps1），保留 `-PullLatest` 逻辑完全不变（git status porcelian 检查 + git pull --rebase 部分不动）。

如果 `$SkipCodex` 被设置，添加 `'-SkipCodex'` 到 `$installArgs`。
如果 `$SkipClaude` 被设置，添加 `'-SkipClaude'` 到 `$installArgs`。

写入后验证两个文件语法: `powershell -NoProfile -Command "Get-Command (Join-Path $PSScriptRoot 'scripts/link_codex_global_skills.ps1') -Syntax"` 和 `powershell -NoProfile -Command "Get-Command (Join-Path $PSScriptRoot 'scripts/update.ps1') -Syntax"`
  </action>
  <acceptance_criteria>
**link_codex_global_skills.ps1:**
- `grep -c '-SkipClaude' scripts/link_codex_global_skills.ps1` 输出 ≥ 1
- `grep -c '-CodexHome' scripts/link_codex_global_skills.ps1` 输出 0 (已移除旧参数)
- `grep -c '-CodexSkillsHome' scripts/link_codex_global_skills.ps1` 输出 ≥ 1
- `grep -c '\.agents\\\\skills' scripts/link_codex_global_skills.ps1` 输出 ≥ 1

**update.ps1:**
- `grep -c '\$CodexHome' scripts/update.ps1` 输出 0 (已从参数移除)
- `grep -c '\$ClaudeHome' scripts/update.ps1` 输出 0 (已从参数移除)
- `grep -c 'PullLatest' scripts/update.ps1` 输出 ≥ 2 (参数声明 + 逻辑保留)
- `grep -c 'install\.ps1' scripts/update.ps1` 输出 ≥ 1 (仍然委托)

**两个文件:**
- PowerShell 语法检查通过，无错误
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -Command "& { Get-Command -Syntax (Join-Path $PSScriptRoot 'scripts/link_codex_global_skills.ps1') -ErrorAction Stop | Out-Null; Get-Command -Syntax (Join-Path $PSScriptRoot 'scripts/update.ps1') -ErrorAction Stop | Out-Null; Write-Host 'PASS: both scripts have valid syntax' }"</automated>
  </verify>
  <done>link_codex_global_skills.ps1 和 update.ps1 已更新，与新的 symlink install.ps1 兼容。两个文件 PowerShell 语法有效。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户执行 install.ps1 | PowerShell 脚本以用户权限运行；参数来自命令行输入 |
| install.ps1 → mklink /J (cmd) | 调用外部 cmd.exe 创建目录 junction；路径来自 $RepoRoot 和 $CodexSkillsHome |
| install.ps1 → 文件系统 (Remove-Item) | -CleanLegacy 删除旧安装产物；路径基于 $env:USERPROFILE |
| install.ps1 → install.json 写入 | 元数据写入 ~/.liam-git-workflow/install.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering | install.ps1 $RepoRoot 参数注入 | mitigate | `$RepoRoot` 在 Resolve-LiamGitWorkflowRepoRoot 中通过 `Resolve-Path -LiteralPath` 解析，无效路径会导致脚本终止。`-LiteralPath` 阻止通配符扩展。pass参数不被解释为表达式。ASVS V5.1.4: 输入验证在系统边界执行。 |
| T-04-12 | Elevation of Privilege | mklink /J 命令路径注入 | mitigate | mklink 的目标路径和源路径来自 `Join-Path` 拼接，不存在外部命令注入向量的可能（无 shell 元字符解释）。`$codexTarget` = Join-Path + 固定名称 "liam-git-workflow"；`$codexSource` = Resolve-Path 结果（已规范化绝对路径）。 |
| T-04-13 | Tampering | -CleanLegacy 删除路径遍历 | mitigate | CleanLegacy 删除的路径均为 Join-Path($env:USERPROFILE, 固定子路径) 构造，不接收用户输入的任意路径。无法通过参数遍历到 $env:USERPROFILE 之外。路径过滤器为硬编码的 "liam-git-workflow*"。符合最小权限原则 (ASVS V1.4.2)。 |
| T-04-14 | Information Disclosure | install.json 元数据写入 | accept | install.json 仅包含版本号、仓库路径、安装时间和安装模式。仓库路径对已访问文件系统的本地进程非秘密。`$env:USERPROFILE\.liam-git-workflow\` 权限继承用户主目录默认 ACL。 |
| T-04-15 | Denial of Service | mklink 目标已存在时的 rmdir | accept | 脚本先 `cmd /c rmdir` 尝试移除 junction，失败后回退到 `Remove-Item -Recurse -Force` 移除目录。最坏情况: 权限不足导致移除失败，脚本抛错终止。不会损坏用户数据（仅操作 `~/.agents/skills/liam-git-workflow` 这个由脚本创建和维护的路径）。 |
| T-04-16 | Spoofing | install.ps1 脚本来源验证 | accept | install.ps1 从仓库 checkout 执行。仓库本身通过 git clone 获取，来源验证由 git 提供（HTTPS/SSH）。脚本内不包含自签名校验或哈希验证 — 这在本地开发工具的威胁模型中是可接受的（ASVS L1 不要求本地开发工具自校验）。 |
</threat_model>

<verification>
验证安装脚本链完整性:
1. `powershell -NoProfile -Command "Get-Command scripts/install.ps1 -Syntax"` — install.ps1 语法有效
2. `powershell -NoProfile -Command ". scripts/common.ps1; Get-Command Resolve-LiamGitWorkflowRepoRoot"` — common.ps1 保留函数可用
3. `powershell -NoProfile -Command "Get-Command scripts/link_codex_global_skills.ps1 -Syntax"` — 兼容脚本语法有效
4. `powershell -NoProfile -Command "Get-Command scripts/update.ps1 -Syntax"` — 更新脚本语法有效
5. install.ps1 不包含 Copy-Item / Copy-DirectoryTree / Copy-FileWithReplacements / .Replace( 调用
6. install.ps1 包含 mklink /J 命令
7. common.ps1 不包含已删除的 5 个函数
</verification>

<success_criteria>
- install.ps1 运行后创建 directory junction 而非复制文件
- git pull 更新仓库后技能立即生效（symlink 模型）
- -CleanLegacy 能清理旧版文件复制产物
- link_codex_global_skills.ps1 和 update.ps1 与新 install.ps1 兼容
- common.ps1 不再包含文件复制或路径重写逻辑
</success_criteria>

<output>
After completion, create `.planning/phases/04-superpowers-codex-claude-code/04-03-SUMMARY.md`
</output>
